# Activity Summary for 3/13/2026

## 1:13:43 PM
The `d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py` file, last modified on 3/13/2026, 11:14:59 AM, shows significant updates to a FastAPI application named "Edge Bet Pro Backend."

Key changes and patterns in `app.py`:

*   **Imports and Initialization**: The application imports various modules for FastAPI, CORS middleware, logging, database initialization, routers (strategies, users, betfair), market data fetching, and scheduling. It initializes loggers, and a `StreamingMarketResolver`.
*   **FastAPI Setup**: An `app = FastAPI()` instance is created with `openapi_tags`. A `custom_openapi` function is defined to add JWT Bearer authentication to the OpenAPI schema for all routes, indicating a focus on secure API endpoints.
*   **Middleware**: CORS middleware is added, allowing requests from all origins (`allow_origins=["*"]`), suggesting a development or internal environment configuration.
*   **Routers**: Routes for `users`, `strategies`, and `betfair` are included. Comments suggest that `demo_ui` and `simulation` routers might be uncommented for development or testing, indicating modularity and different deployment contexts.
*   **Startup Events (`@app.on_event("startup")`)**:
    *   Logging is emphasized with an initial `logger.info` message.
    *   `init_db()` is called to initialize the database.
    *   `fetch_and_store_market_catalogs()` is called directly on startup.
    *   The `start_scheduler()` function is intended to be called, although it is currently commented out, suggesting a transition or change in how background tasks are managed.
    *   Several other previously active background processes like `start_streaming`, `start_scrapping`, and `run_betting_logic_once` are commented out, pointing to a shift towards scheduler-managed tasks or a temporary disablement.
*   **API Endpoints**:
    *   `/`: A basic "EdgeBetPro" root endpoint.
    *   `/health`: A health check endpoint returning "up".
    *   `/scheduler`: A new endpoint to check the status of both background and async schedulers, returning detailed status messages (e.g., "Both the schedulers are running," "Only background scheduler running"). This indicates a focus on monitoring background task execution.
*   **Scheduler Implementation (`start_scheduler` function)**:
    *   The `start_scheduler` function is now `async`.
    *   It initializes both a `BackgroundScheduler` (for synchronous jobs) and an `AsyncIOScheduler` (for asynchronous jobs).
    *   **Background Scheduler Jobs**:
        *   `fetch_and_store_market_catalogs`: Triggered by `IntervalTrigger` every `MARKET_UPDATE_INTERVAL_MINUTES` (likely 30 minutes, based on the .env file).
        *   `run_market_settlement_job`: Triggered every 1 minute.
    *   **AsyncIO Scheduler Jobs**:
        *   `auto_scheduler`: Triggered every 1 minute for "auto_start_betting".
        *   `fetch_pending_outcome_markets`: Triggered every 30 minutes.
    *   Both schedulers start immediately (`next_run_time=datetime.now(timezone.utc)`).
    *   Comprehensive error handling is included for scheduler startup.
*   **Shutdown Events (`@app.on_event("shutdown")`)**: Schedulers are gracefully shut down when the application stops, ensuring clean resource release.

**Overall patterns in `app.py`**:
*   The application uses FastAPI with a focus on structured routing and API documentation (`openapi_tags`, `custom_openapi`).
*   It implements robust background task scheduling using both `BackgroundScheduler` and `AsyncIOScheduler` for various data fetching, processing, and betting logic tasks.
*   There's a clear emphasis on logging and monitoring, including a scheduler status endpoint.
*   Many previously active background processes for streaming, scraping, and betting logic have been commented out, suggesting a transition towards a more centralized, scheduler-driven approach for these operations, potentially to address "scraping_issue" as implied by the project path.
*   The extensive commenting out of certain lines, especially in the `startup` event, suggests an active development phase where functionalities are being refined or re-architected.