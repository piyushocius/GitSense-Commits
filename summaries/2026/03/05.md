# Activity Summary for 3/5/2026

## 2:28:44 PM
The file `d:\Projects\KolossusAI\frontend\lib\features\chat\data\services\chat_websocket_service.dart`, last updated on `3/2/2026, 9:17:48 PM`, defines a `ChatWebSocketService` responsible for real-time communication within a chat feature.

This service manages a comprehensive WebSocket connection lifecycle, including connecting, reconnecting, and disconnecting, while handling various states such as `disconnected`, `connecting`, `connected`, `reconnecting`, and `error`. It features robust authentication, obtaining fresh access tokens via a `TokenGetter` and sending them securely: in headers for mobile/desktop `IOWebSocketChannel` connections (to avoid 403 errors due to lack of cookies) and as an `authenticate` message over the channel for all platforms.

The service supports an extensive array of `WebSocketMessageType`s, indicating a rich feature set beyond basic chat. These types categorize messages for:
*   Core chat functionalities (`status`, `message`, `error`, `clearChat`).
*   Authentication events (`authSuccess`, `authError`).
*   Process management (`processOutput`, `processStatus`, `processList`, `processStarted`, `processStopped`, `processError`, `executionStarted`, `executionCompleted`, `executionCancelled`).
*   Multi-agent activities (`agentActivity`, `agentQuestion`, `agentAnswer`, `agentDiscovery`).
*   Tool lifecycle events (`toolLifecycle`).
*   Intelligent multi-agent collaboration (`agentChat`, `subtaskCompletion`, `approvalRequest`).
*   Workforce unread notifications (`workforceUnread`).

A significant pattern in the code is the extensive use of `StreamController.broadcast()` for each specific message type (e.g., `_messageController`, `_processOutputController`, `_agentActivityController`). This design allows for a highly reactive architecture where different parts of the application can listen to specific streams of events without tight coupling. The service intelligently adapts its connection method based on the platform (`kIsWeb`), utilizing `IOWebSocketChannel` for mobile/desktop and `WebSocketChannel.connect` for web. It includes logic for automatic reconnection with exponential backoff and a ping mechanism, enhancing connection stability. The provided code snippet also indicates the capability to send chat messages with optional AI model IDs and file attachments, though the implementation details for sending are truncated.