# Activity Summary for 2/27/2026

## 2:22:04 PM
The provided log details changes across two distinct projects: `edge-bet-pro-ui` (an Angular frontend) and `edge-bet-pro-backend` (a FastAPI backend).

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\Dockerfile`**

*   **Timestamp:** Changes were made frequently between 2/27/2026, 1:22:28 PM and 2/27/2026, 1:23:21 PM.
*   **Key Updates:** The Dockerfile is configured to build and run an Angular application.
    *   It uses `node:22.14.0` as the base image.
    *   Installs Angular CLI `19.0.6` globally.
    *   Copies `package.json` for dependency installation (`npm install --force --silent`).
    *   Creates a `service` user with UID 1003 and sets permissions for `/app/.angular` and `/app/logs`.
    *   Switches to the `service` user.
    *   Exposes port `4200`.
    *   The `CMD` command starts the Angular development server: `ng serve --host 0.0.0.0 --port 4200 --public-host flutterbot.internal:4200`.
*   **Patterns/Recurring Elements:** The core Dockerfile instructions remained constant across these updates. The changes primarily involved refining comments related to the `service` user creation and `CMD` instructions, specifically clarifying that certain steps might not be required for a "local run." There was also a commented-out alternative `CMD` command for local use, which was refined to explicitly state "Use for local run."

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\src\environments\environment.ts`**

*   **Timestamp:** This file was modified multiple times between 2/27/2026, 1:24:50 PM and 2/27/2026, 1:52:47 PM.
*   **Key Updates:** This file defines environment-specific configuration for the Angular application, including `appVersion`, `production` status, a `server` URL, and an API `prefix`.
*   **Patterns/Recurring Elements:** The primary changes in this file involved frequent adjustments to the `server` URL. The log indicates several modifications to both the IP address and port used for the backend server, suggesting active configuration testing or adjustments during development.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\main.py`**

*   **Timestamp:** 2/27/2026, 1:45:00 PM.
*   **Key Updates:** This script serves as the entry point for the FastAPI backend.
    *   It uses `uvicorn` to run the `app` imported from `api.app`.
    *   The application is configured to listen on `host="0.0.0.0"` and `port=8007`.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py`**

*   **Timestamp:** 2/27/2026, 1:45:40 PM.
*   **Key Updates:** This is the main FastAPI application definition, setting up the backend services.
    *   **Core Setup:** Initializes FastAPI with CORS middleware configured to allow all origins, credentials, methods, and headers. It also includes custom OpenAPI schema generation with JWT authentication.
    *   **Routers:** Integrates routers for `users`, `strategies`, and `betfair` functionalities. Comments indicate `simulation` and `demo_ui` routers are present but commented out, likely for development/testing flexibility.
    *   **Startup Logic (`@app.on_event("startup")`):**
        *   Initializes the database (`init_db()`).
        *   Starts a daemon thread to run `run_betting_logic_once()` asynchronously.
        *   Includes comprehensive logging, reporting startup and log file location.
    *   **Endpoints:** Provides basic endpoints for root (`/`), health checks (`/health`), and scheduler status (`/scheduler`).
    *   **Scheduling (`start_scheduler()`):**
        *   Configures and starts two types of schedulers using `APScheduler`:
            *   `BackgroundScheduler`: For synchronous tasks like `fetch_and_store_market_catalogs` (interval based on `MARKET_UPDATE_INTERVAL_MINUTES`) and `run_market_settlement_job` (every 1 minute).
            *   `AsyncIOScheduler`: For asynchronous tasks like `auto_scheduler` (every 1 minute) and `fetch_pending_outcome_markets` (every 30 minutes).
        *   Includes error handling for scheduler startup.
    *   **Shutdown Logic (`@app.on_event("shutdown")`):** Ensures both schedulers are gracefully shut down when the application stops.
*   **Patterns/Recurring Elements:** The file demonstrates a robust FastAPI application structure, emphasizing modularity through routers, comprehensive logging, scheduled background tasks for data fetching and processing (market catalogs, betting logic, market settlement, pending outcome markets), and proper application lifecycle management (startup/shutdown events). The use of both `BackgroundScheduler` and `AsyncIOScheduler` highlights the need to manage both synchronous and asynchronous periodic tasks within the application.

## 6:22:00 PM
The changes primarily focus on configuration and setup for both the frontend (Angular UI) and backend (FastAPI).

**File-specific Updates:**

*   **`d:\Projects\scraping_issue\edge-bet-pro-ui\Dockerfile` (Multiple entries between 2/27/2026, 1:22:28 PM and 1:23:21 PM):**
    *   The Dockerfile consistently defines an Angular UI build process using Node.js 22.14.0 and Angular CLI 19.0.6.
    *   It sets up a working directory `/app`, installs dependencies using `npm install --force --silent`, and copies the project files.
    *   A `service` user with UID 1003 is created, and permissions are set for `/app/.angular` and `/app/logs`. The instruction `USER service` is used, but comments around it (`(Not r)`, `(Not requu)`, `(Not required )`, `(Not required for local run)`) evolve over these timestamps, indicating a clarification or decision to make this optional or understood for local development.
    *   Port 4200 is exposed.
    *   The `CMD` instruction primarily starts the Angular development server: `ng serve --host 0.0.0.0 --port 4200 --public-host flutterbot.internal:4200`.
    *   A commented-out alternative `CMD ["ng", "serve", "--host", "0.0.0.0", "--port", "4200"]` is added and later annotated with "Use for local run" indicating a distinction between internal/production host and local development. The comment `ng serve --host 0.0.0.0 --port 4300` also remains present, possibly as a note for alternative local port usage.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\main.py` (2/27/2026, 1:45:00 PM):**
    *   The Uvicorn server is configured to run the FastAPI application (`api.app`) on host `0.0.0.0` and port `8007`. This indicates a change in the backend's exposed port.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py` (2/27/2026, 1:45:40 PM):**
    *   This file defines the core FastAPI application for the backend.
    *   It imports various modules for strategies, users, Betfair integration, database management, logging, streaming, and market data scraping.
    *   CORS middleware is configured to allow all origins, credentials, methods, and headers.
    *   Custom OpenAPI schema is implemented for JWT authentication.
    *   On startup, comprehensive logging is set up, the database is initialized, and a betting logic thread is started to run every 5 seconds.
    *   A `start_scheduler` async function is defined (though not directly called in `on_startup` in the final shown state, it's part of the application logic), configuring two schedulers:
        *   `BackgroundScheduler`: Adds jobs to `fetch_and_store_market_catalogs` every 30 minutes and `run_market_settlement_job` every 1 minute.
        *   `AsyncIOScheduler`: Adds jobs for `auto_scheduler` every 1 minute and `fetch_pending_outcome_markets` every 30 minutes.
    *   `health` and `scheduler` endpoints are included for monitoring.
    *   On shutdown, both schedulers are gracefully shut down.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\migration_script_1_rating_20_cleanup.py` (2/27/2026, 2:25:00 PM and 2:57:52 PM):**
    *   This script implements a database migration for "Rating 20 Cleanup" on the `tabs_data` table.
    *   It targets records where any "Open" runner has a `dfsFormRating` of `20`.
    *   For such records, it sets `race_strength`, `betfair_win_value_runner`, and `betfair_place_value_runner` to `None`.
    *   Additionally, within the `estimated_value_runner` JSON field, it sets `win_value_runner` and `place_value_runner` to `None` for all runners.
    *   The script logs changes to a CSV file in an `exports` directory. The two entries for this file are identical, indicating no changes between these timestamps.

**Recurring Elements and Patterns:**

*   **Timestamp Clusters:** There are distinct bursts of activity. The Dockerfile changes occur within a minute (1:22 PM to 1:23 PM), indicating rapid iteration on deployment configuration or comments. Backend configuration changes (`main.py`, `app.py`) also happen around 1:45 PM, followed by the migration script at 2:25 PM and 2:57 PM.
*   **Port Changes:** There's a clear shift in backend port from `8005` (implied from earlier `environment.ts` entries not summarized, but reflected in comments) to `8007` as seen in `main.py`.
*   **Comment Evolution:** Comments in the Dockerfile, particularly around the `USER service` instruction, show an iterative clarification process from brief notes to more descriptive explanations like "(Not required for local run)". Similarly, the commented `CMD` for local runs shows a refinement in documentation.
*   **Focus on Development/Deployment:** The changes in Dockerfile and backend configuration suggest ongoing work to refine the development and deployment process, including host configurations, user permissions, and service scheduling.
*   **Scheduled Background Tasks:** The `app.py` update introduces robust background scheduling for market data fetching, betting logic, and market settlement, indicating a move towards more automated and real-time operations.