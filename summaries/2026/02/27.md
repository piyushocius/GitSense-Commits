# Activity Summary for 2/27/2026

## 2:22:04 PM
The provided log details changes across two distinct projects: `edge-bet-pro-ui` (an Angular frontend) and `edge-bet-pro-backend` (a FastAPI backend).

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\Dockerfile`**

*   **Timestamp:** Changes were made frequently between 2/27/2026, 1:22:28 PM and 2/27/2026, 1:23:21 PM.
*   **Key Updates:** The Dockerfile is configured to build and run an Angular application.
    *   It uses `node:22.14.0` as the base image.
    *   Installs Angular CLI `19.0.6` globally.
    *   Copies `package.json` for dependency installation (`npm install --force --silent`).
    *   Creates a `service` user with UID 1003 and sets permissions for `/app/.angular` and `/app/logs`.
    *   Switches to the `service` user.
    *   Exposes port `4200`.
    *   The `CMD` command starts the Angular development server: `ng serve --host 0.0.0.0 --port 4200 --public-host flutterbot.internal:4200`.
*   **Patterns/Recurring Elements:** The core Dockerfile instructions remained constant across these updates. The changes primarily involved refining comments related to the `service` user creation and `CMD` instructions, specifically clarifying that certain steps might not be required for a "local run." There was also a commented-out alternative `CMD` command for local use, which was refined to explicitly state "Use for local run."

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\src\environments\environment.ts`**

*   **Timestamp:** This file was modified multiple times between 2/27/2026, 1:24:50 PM and 2/27/2026, 1:52:47 PM.
*   **Key Updates:** This file defines environment-specific configuration for the Angular application, including `appVersion`, `production` status, a `server` URL, and an API `prefix`.
*   **Patterns/Recurring Elements:** The primary changes in this file involved frequent adjustments to the `server` URL. The log indicates several modifications to both the IP address and port used for the backend server, suggesting active configuration testing or adjustments during development.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\main.py`**

*   **Timestamp:** 2/27/2026, 1:45:00 PM.
*   **Key Updates:** This script serves as the entry point for the FastAPI backend.
    *   It uses `uvicorn` to run the `app` imported from `api.app`.
    *   The application is configured to listen on `host="0.0.0.0"` and `port=8007`.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py`**

*   **Timestamp:** 2/27/2026, 1:45:40 PM.
*   **Key Updates:** This is the main FastAPI application definition, setting up the backend services.
    *   **Core Setup:** Initializes FastAPI with CORS middleware configured to allow all origins, credentials, methods, and headers. It also includes custom OpenAPI schema generation with JWT authentication.
    *   **Routers:** Integrates routers for `users`, `strategies`, and `betfair` functionalities. Comments indicate `simulation` and `demo_ui` routers are present but commented out, likely for development/testing flexibility.
    *   **Startup Logic (`@app.on_event("startup")`):**
        *   Initializes the database (`init_db()`).
        *   Starts a daemon thread to run `run_betting_logic_once()` asynchronously.
        *   Includes comprehensive logging, reporting startup and log file location.
    *   **Endpoints:** Provides basic endpoints for root (`/`), health checks (`/health`), and scheduler status (`/scheduler`).
    *   **Scheduling (`start_scheduler()`):**
        *   Configures and starts two types of schedulers using `APScheduler`:
            *   `BackgroundScheduler`: For synchronous tasks like `fetch_and_store_market_catalogs` (interval based on `MARKET_UPDATE_INTERVAL_MINUTES`) and `run_market_settlement_job` (every 1 minute).
            *   `AsyncIOScheduler`: For asynchronous tasks like `auto_scheduler` (every 1 minute) and `fetch_pending_outcome_markets` (every 30 minutes).
        *   Includes error handling for scheduler startup.
    *   **Shutdown Logic (`@app.on_event("shutdown")`):** Ensures both schedulers are gracefully shut down when the application stops.
*   **Patterns/Recurring Elements:** The file demonstrates a robust FastAPI application structure, emphasizing modularity through routers, comprehensive logging, scheduled background tasks for data fetching and processing (market catalogs, betting logic, market settlement, pending outcome markets), and proper application lifecycle management (startup/shutdown events). The use of both `BackgroundScheduler` and `AsyncIOScheduler` highlights the need to manage both synchronous and asynchronous periodic tasks within the application.