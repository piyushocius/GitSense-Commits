# Activity Summary for 2/9/2026

## 2:50:15 PM
The provided log consists of multiple entries for the file `d:\Projects\KolossusAI\connector\management\commands\seed_connectors.py` across a very short period on February 9, 2026, specifically between 2:26:07 PM and 2:28:34 PM. All logged entries for this file show identical content, indicating either no actual changes occurred between these timestamps, or the log captured multiple saves of the exact same code.

This Python file is a Django management command (`seed_connectors.py`) used to populate a database with predefined `Connector` configurations for an AI system.

**Key information from the file:**

*   **System Instructions for AI (Constants):** The file defines numerous string constants, each providing specific "system instructions" for an AI on how to interact with different third-party services using user credentials. These include:
    *   **Generic Connectors:** GitHub, Google Drive, Dropbox, Impact, Partnerize, Salesforce, dbt Cloud, AWS, Databricks, HubSpot, Microsoft Outlook, Microsoft Teams, PostgreSQL. Most of these state to "do whatever the user requests," with some offering specific capabilities (e.g., Jira for managing issues/projects, dbt for triggering jobs, Postgres for querying data).
    *   **Detailed Connectors:**
        *   **Jira:** Includes instructions for handling multiple Jira sites via environment variables (`SITE_COUNT`, `SITE_N_CLOUD_ID`) and provides the API base URL.
        *   **AWS:** Advises checking `available_services` in metadata for accessible services.
        *   **PostgreSQL:** Explicitly warns to "Always be careful with write operations and confirm with the user before modifying data."
        *   **Meta Ads (Facebook Ads):** Provides a detailed list of "AVAILABLE DATA" (e.g., Ad Accounts, Campaigns, Insights, Audiences), guidance on handling "MULTIPLE AD ACCOUNTS" through metadata, and specific "API USAGE" notes (Meta Graph API v21.0, 'act_' prefix for account IDs, date_preset and breakdowns for insights).
        *   **Awin:** Instructs to respect `account_type` and `account_id` in metadata.
        *   **CJ Affiliate:** Details on using `publisher_id` and `property_id`, handling optional `PROPERTY ID`, distinguishing `ACCOUNT TYPE` (advertiser vs. publisher), and a `PUBLISHER REST NOTE` regarding XML responses and `requestor-cid`.
        *   **Rakuten Advertising:** Specifies the correct API base URL and links to API documentation, emphasizing the importance of using endpoints exactly as listed.

*   **`Command` Class for Seeding:**
    *   The `handle` method defines a list of dictionaries, `connectors`, intended to populate the `Connector` model.
    *   **LinkedIn Connector:** Configured for OAuth with `client_id` and `client_secret` as empty placeholders, and scopes including `openid`, `profile`, `email`. It requires tenant setup.
    *   **Impact.com Connector:** Configured with `auth_type` as `api_token` and references the `IMPACT_SYSTEM_INSTRUCTION` constant. It does not require tenant setup.

**Patterns and Recurring Elements:**

*   **AI Integration Focus:** A dominant pattern is the explicit instruction for an AI to use credentials for various services, outlining capabilities and often specific operational details or caveats for the AI.
*   **Consistent Instruction Format:** Most system instructions begin with "Use these credentials to connect with the user's [Service Name] account and do whatever the user requests."
*   **Metadata Reliance:** Several instructions (AWS, Meta Ads, Awin, CJ) emphasize checking or respecting specific metadata fields (e.g., `available_services`, `ad_accounts`, `account_type`, `publisher_id`) to tailor API calls or access permissions.
*   **Placeholders:** A comment notes that `{WORKFORCE_ID}` is a placeholder replaced at runtime. The `client_id` and `client_secret` fields in the LinkedIn connector are also empty strings, implying they are filled in dynamically or from environment variables rather than hardcoded.
*   **Connector Definition Structure:** Each connector defined in the `connectors` list follows a consistent dictionary schema, including `name`, `slug`, `description`, `icon_url`, `category`, `sort_order`, `requires_tenant_setup`, and `config`. Some also explicitly link to a `system_instruction` constant.

## 3:50:12 PM
The provided logs all pertain to changes within the file `d:\Projects\KolossusAI\connector\management\commands\seed_connectors.py`. All entries span a very short period on 2/9/2026, between 2:26:07 PM and 2:28:34 PM, suggesting minor saves or edits without substantial functional changes reflected in the provided code snippets, as the content across all timestamps is identical.

This Python file is a Django management command (`seed_connectors.py`) designed to populate or "seed" a database with various connector configurations.

Key information from the file:
*   **System Instructions for AI**: The file defines a series of `_SYSTEM_INSTRUCTION` constants for various services (e.g., GITHUB, GOOGLE_DRIVE, DROPBOX, IMPACT, SALESFORCE, JIRA, DBT, AWS, DATABRICKS, HUBSPOT, MICROSOFT_OUTLOOK, MICROSOFT_TEAMS, POSTGRES, META_ADS, AWIN, CJ, RAKUTEN). These strings serve as detailed guidance for an AI on how to interact with these third-party services using provided credentials.
*   **Detailed AI Prompts**: Many system instructions provide specific context for AI agents, including:
    *   **Service capabilities**: What the AI can do (e.g., manage Jira issues, trigger dbt jobs, access S3/EC2 for AWS, manage CRM for HubSpot, send emails for Outlook, manage ad accounts/campaigns for Meta Ads).
    *   **API usage specifics**: Details like Meta Graph API version (v21.0), ad account ID prefixes (`act_`), insight parameters, Jira API base URL, Rakuten API base URL and documentation link.
    *   **Multi-account/site handling**: Instructions for Jira (using `SITE_N_CLOUD_ID` environment variables) and Meta Ads (checking `ad_accounts` metadata) for accessing multiple instances.
    *   **Safeguards**: Warnings for PostgreSQL regarding write operations and confirmations, and for AWS to check `available_services` in metadata.
    *   **Placeholder for Workforce ID**: `{WORKFORCE_ID}` is noted as a runtime placeholder in the general comment.
*   **Connector Configuration List**: The `handle` method in the `Command` class initializes a list of dictionaries named `connectors`. Each dictionary represents a specific service connector.
    *   The snippet shows configurations for "LinkedIn" and "Impact.com".
    *   Each connector specifies `name`, `slug`, `description`, `icon_url`, `category`, `sort_order`, `requires_tenant_setup` (boolean), and a `config` dictionary.
    *   **LinkedIn** is configured for OAuth with empty `client_id` and `client_secret` placeholders, and defined `scopes` (`openid`, `profile`, `email`). It requires tenant setup.
    *   **Impact.com** uses `api_token` for authentication, does not require tenant setup, and explicitly links to `IMPACT_SYSTEM_INSTRUCTION` for its AI guidance.

**Patterns and Recurring Elements**:
*   A consistent pattern of defining AI system instructions as string constants for each service.
*   The structure of these instructions often begins with "Use these credentials to connect with the user's [Service Name] account and do whatever the user requests."
*   Connectors are defined with a uniform dictionary structure, including metadata like `name`, `category`, and configuration details.
*   The use of `_SYSTEM_INSTRUCTION` constants within the `connectors` list links the configuration data directly to the AI's operational guidance.
*   Placeholder `client_id` and `client_secret` are defined as empty strings in the configuration for OAuth-based connectors, indicating where actual credentials would be dynamically inserted or provided during setup.

## 11:50:11 PM
The provided log entries for `d:\Projects\KolossusAI\connector\management\commands\seed_connectors.py` show that the file's content remained unchanged across all recorded timestamps between 2/9/2026, 2:26:07 PM, and 2/9/2026, 2:28:34 PM. No actual code modifications were introduced during this period.

The file itself is a Django management command (`seed_connectors.py`) designed to seed various AI connectors into a system. Key aspects of its content include:

*   **System Instructions for AI:** A significant portion of the file is dedicated to defining `_SYSTEM_INSTRUCTION` strings for a wide array of services such as GitHub, Google Drive, Dropbox, Impact, Partnerize, Salesforce, Jira, dbt Cloud, AWS, Databricks, HubSpot, Microsoft Outlook, Microsoft Teams, PostgreSQL, Meta Ads, Awin, CJ Affiliate, and Rakuten Advertising. These instructions guide an AI on how to use provided credentials and interact with each service.
*   **Detailed API Usage Notes:** Several `_SYSTEM_INSTRUCTION` strings provide specific details for AI interaction:
    *   **Jira:** Instructions for handling multiple Jira sites using environment variables (`SITE_COUNT`, `SITE_N_CLOUD_ID`, `SITE_N_NAME`, `SITE_N_URL`) and specifying the API base URL.
    *   **AWS:** Emphasizes checking `available_services` in metadata for accessible services.
    *   **Meta Ads:** Details available data (ad accounts, campaigns, ad sets, ads, insights, audiences), notes on handling multiple ad accounts via metadata, and specific API usage guidelines (Graph API v21.0, 'act_' prefix for account IDs, `date_preset`, and breakdowns).
    *   **Awin:** Directs the AI to respect `account_type` and `account_id` from metadata.
    *   **CJ Affiliate:** Advises on `publisher_id`, `property_id`, `account_type` (advertiser vs. publisher), and specific requirements for REST endpoints like `Accept: application/xml` and `requestor-cid`.
    *   **Rakuten Advertising:** Provides the correct API base URL and links to documentation, explicitly warning against guessing or inventing URLs.
*   **Connector Definitions:** The `handle` method defines a list of dictionaries, each representing a connector. The example snippets show "LinkedIn" and "Impact.com" connectors, specifying:
    *   `name`, `slug`, `description`, `icon_url`, `category`, `sort_order`.
    *   `requires_tenant_setup`: A boolean indicating if tenant-specific setup is needed.
    *   `system_instruction`: A reference to one of the predefined `_SYSTEM_INSTRUCTION` variables.
    *   `config`: Configuration details, such as `client_id`, `client_secret`, and `scopes` for OAuth (e.g., LinkedIn), or `auth_type: "api_token"` (e.g., Impact.com).
*   **Recurring Patterns:** A common pattern across the `_SYSTEM_INSTRUCTION` messages is the phrase "Use these credentials to connect with the user's [Service Name] account and do whatever the user requests," followed by service-specific capabilities or caveats. Many instructions also mention checking metadata for specific details relevant to API calls (e.g., `available_services`, `ad_accounts`, `account_type`, `account_id`).