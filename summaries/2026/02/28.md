# Activity Summary for 2/28/2026

## 3:22:10 AM
The changes primarily affect the deployment configuration of the `edge-bet-pro-ui` frontend application, the core `edge-bet-pro-backend` FastAPI application logic, and a database migration script for the backend.

**File-Specific Updates:**

*   **`d:\Projects\scraping_issue\edge-bet-pro-ui\Dockerfile`**
    *   Multiple minor updates occurred between **2/27/2026, 1:22:28 PM** and **2/27/2026, 1:23:21 PM**.
    *   The Dockerfile consistently uses Node.js `22.14.0` as the base image and installs Angular CLI `19.0.6`. It configures the build to copy `package.json` first for caching, installs dependencies with `--force --silent`, and then copies the entire project.
    *   It defines a `service` user with UID `1003` and sets permissions for Angular-related directories (`/app/.angular`, `/app/logs`), then switches to this user.
    *   Port `4200` is exposed, and the Angular development server is started with `CMD ["ng", "serve", "--host", "0.0.0.0", "--port", "4200", "--public-host", "flutterbot.internal:4200"]`.
    *   The changes throughout this period were mainly clarifications in comments regarding the `service` user setup and `USER service` instruction, explicitly stating that these might not be required for local runs.
    *   At **2/27/2026, 1:23:09 PM**, an alternative `CMD` (`ng serve --host 0.0.0.0 --port 4200`) was commented out, suggesting consideration for different deployment or local development scenarios, further clarified by comments like "Use for local run" in subsequent updates.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\main.py`**
    *   At **2/27/2026, 1:45:00 PM**, this file was configured to run the FastAPI application `api.app` using Uvicorn, binding it to all network interfaces (`0.0.0.0`) on port `8007`.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py`**
    *   Updated at **2/27/2026, 1:45:40 PM**.
    *   This file defines the FastAPI application with comprehensive setup, including `CORSMiddleware` (allowing all origins/methods) and custom OpenAPI documentation to enforce JWT `BearerAuth`.
    *   It integrates various modules and routers for users, strategies, and Betfair data.
    *   On application startup (`@app.on_event("startup")`), it initializes the database and explicitly starts a daemon thread to execute `run_betting_logic_once()`. Notably, several previously enabled startup tasks (e.g., `start_scheduler`, data streaming, scraping) are commented out, indicating a focused approach to initial betting logic execution.
    *   The file defines `/`, `/health`, and `/scheduler` endpoints for basic status checks.
    *   An asynchronous `start_scheduler` function is defined (though not called in the current startup event) which configures `BackgroundScheduler` for synchronous jobs (like fetching market catalogs every `MARKET_UPDATE_INTERVAL_MINUTES` and bet settlement every 1 minute) and `AsyncIOScheduler` for asynchronous tasks (like `auto_scheduler` and `fetch_pending_outcome_markets` every 1 and 30 minutes respectively). A shutdown event ensures these schedulers are properly terminated.

*   **`d:\Projects\scraping_issue\edge-bet-pro-backend\migration_script_1_rating_20_cleanup.py`**
    *   This file appeared identically at **2/27/2026, 2:25:00 PM** and **2/27/2026, 2:57:52 PM**.
    *   It's a database migration script targeting the `tabs_data` table.
    *   Its purpose is to identify records where any "Open" runner in the `estimated_value_runner` JSON has a `dfsFormRating` of `20`.
    *   For such identified records, it sets `race_strength`, `betfair_win_value_runner`, and `betfair_place_value_runner` to `None`. Furthermore, it nullifies `win_value_runner` and `place_value_runner` for *all* runners within the `estimated_value_runner` JSON.
    *   The script also updates the `updated_at` timestamp for modified records and generates a CSV log of changes, including the state before the update. It explicitly states that it only modifies `tabs_data` and leaves other tables untouched.

**Patterns and Recurring Elements:**

*   **Local Development vs. Production:** There's a recurring theme of differentiating configurations or instructions for "local run" versus potentially production or internal environments, particularly evident in the Dockerfile comments.
*   **Port/Host Configuration Adjustments:** Changes in `main.py` indicate active tuning of the backend server's port (`8007`) and host binding (`0.0.0.0`).
*   **Comment Evolution:** For the Dockerfile, comments were iteratively refined over very short time intervals, suggesting active, detailed clarification during development.
*   **Backend Focus:** The `app.py` changes show a consolidation or refactoring of backend startup logic, particularly around how betting logic and data fetching/scheduling are initiated, with several previously active components now commented out.
*   **Data Integrity and Cleanup:** The migration script highlights a focus on maintaining data quality within the `tabs_data` table by systematically nullifying specific fields based on a `dfsFormRating` condition.

## 2:22:14 PM
The following details the key changes across different files, highlighting their purpose and timestamps, with an emphasis on recurring patterns.

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\Dockerfile`**
This Dockerfile, belonging to the `edge-bet-pro-ui` project, underwent several minor updates between 1:22 PM and 1:23 PM on 2/27/2026.
*   The Dockerfile consistently uses Node.js 22.14.0 as the base image, installs Angular CLI 19.0.6, and runs `npm install --force --silent` to manage dependencies.
*   It sets up a `service` user with UID 1003 and appropriate directory permissions.
*   The primary `CMD` executes `ng serve --host 0.0.0.0 --port 4200 --public-host flutterbot.internal:4200`.
*   Changes observed were primarily clarifications in comments. Initially, comments related to the `service` user creation were refined from `(Not r)` to `(Not requu)`, then `(Not required )`, and finally `(Not required for local run)`.
*   A commented-out alternative `CMD ["ng", "serve", "--host", "0.0.0.0", "--port", "4200"]` was added, with subsequent comments clarifying its purpose for local runs.

**File: `d:\Projects\scraping_issue\edge-bet-pro-ui\src\environments\environment.ts`**
This Angular environment configuration file was subject to frequent modifications of the `server` endpoint between 1:24 PM and 1:52 PM on 2/27/2026.
*   The `server` variable, which specifies the backend API URL, was repeatedly changed, cycling through `http://192.168.4.4:8005`, `http://0.0.0.0:8005`, `http://0.0.0.0:8007`, `http://127.0.0.0:8007`, and `http://127.0.0.1:8007`, eventually reverting to `http://192.168.4.4:8005`.
*   Minor corrections and adjustments were made to associated comments, such as changing `#For local rubn` to `#For local run`, indicating an ongoing effort to clarify local development configurations.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\main.py`**
At 1:45:00 PM on 2/27/2026, this file defined the entry point for the `edge-bet-pro-backend` FastAPI application.
*   It configures `uvicorn` to run the application (`app` from `api.app`) on `host="0.0.0.0"` and `port=8007`.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\api\app.py`**
This central backend application file was logged at 1:45:40 PM on 2/27/2026, revealing a comprehensive setup.
*   It uses FastAPI, includes `CORSMiddleware` (allowing all origins), integrates database functionalities, and defines multiple routers for `users`, `strategies`, and `betfair`.
*   A custom OpenAPI schema is implemented to support JWT authentication.
*   The `on_startup` event initializes the database and launches `run_betting_logic_once()` in a daemon thread, with a log message indicating "Betting logic scheduled to run every 5 seconds."
*   It defines a health endpoint (`/health`) and a scheduler status endpoint (`/scheduler`).
*   The `start_scheduler` function (intended to be asynchronous) sets up two schedulers:
    *   `BackgroundScheduler` for synchronous tasks like `fetch_and_store_market_catalogs` (interval based on `MARKET_UPDATE_INTERVAL_MINUTES`) and `run_market_settlement_job` (every 1 minute).
    *   `AsyncIOScheduler` for asynchronous tasks such as `auto_scheduler` (every 1 minute) and `fetch_pending_outcome_markets` (every 30 minutes).
*   A `shutdown_event` is configured to gracefully shut down both schedulers.

**File: `d:\Projects\scraping_issue\edge-bet-pro-backend\migration_script_1_rating_20_cleanup.py`**
This migration script appeared in the logs at 2:25:00 PM and 2:57:52 PM on 2/27/2026, with identical content.
*   Its purpose is to clean up `tabs_data` records where any "Open" runner has a `dfsFormRating` of 20.
*   For such records, it sets `race_strength`, `betfair_win_value_runner`, and `betfair_place_value_runner` to `None`. Additionally, within the `estimated_value_runner` JSON, `win_value_runner` and `place_value_runner` for all runners are set to `None`.
*   The script generates a detailed CSV log file in an `exports` directory, documenting the `tabs_data_id`, UUID, event details, and original values before the update.

**Patterns and Recurring Elements:**
*   **Focused Development Period:** All recorded changes occurred on 2/27/2026, indicating a concentrated development or debugging session.
*   **Environment Configuration Adjustments:** There's a clear pattern of frequently adjusting network-related configurations, particularly server IP addresses and ports (`0.0.0.0`, `127.0.0.0`, `127.0.0.1`, `192.168.4.4`, and ports `8005`, `8007`) in `environment.ts`. This, along with comments in the `Dockerfile` about "local run," suggests ongoing efforts to fine-tune network communication between the UI and backend, likely for different deployment environments (local vs. internal/staging).
*   **"EdgeBetPro" Project:** The name "EdgeBetPro" consistently identifies the project across UI (Dockerfile comments) and backend (`app.py` logs, root endpoint).
*   **Backend Orchestration:** The `app.py` file demonstrates a robust backend structure with extensive use of FastAPI features, scheduled tasks (`apscheduler`), and dedicated threads for critical logic (`run_betting_logic_once`), highlighting a system designed for real-time or near-real-time data processing and betting operations.
*   **Data Integrity and Cleanup:** The presence of a specific migration script for "Rating 20 Cleanup" suggests a focus on maintaining data quality or addressing specific data conditions within the `tabs_data` table.
*   **Comment Refinements:** Minor but recurring changes to comments in `Dockerfile` and `environment.ts` indicate an iterative process of improving clarity and documentation for configuration specifics.