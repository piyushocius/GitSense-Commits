# Activity Summary for 6/18/2026

## 10:49:46 AM
The file `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`, last updated on `6/17/2026, 11:53:22 AM`, contains detailed system instructions for a Database Agent.

This agent is tasked with processing Parsec print estimate PDFs, extracting job data, and inserting it into a SQL Server database using `pymssql`. The instructions emphasize direct execution and strict adherence to defined steps, without extended reasoning.

Key updates and rules highlighted in this file include:

*   **Data Integrity and Validation**: Strict rules are in place to prevent `NULL` insertions (requiring `''` for text and `'0'` for numeric), ensure validation of Tier 1 fields, and stop processing to query the user if validation fails. It also mandates inserting all 282 `ProjectServiceValue` rows before calling a stored procedure.
*   **Multiple PDF Handling (Batch Processing)**: Comprehensive guidelines are provided for processing multiple PDFs sequentially in a batch. This includes announcing the processing order, strictly completing all steps for one PDF before moving to the next, re-querying job (JN) and estimate (EN) numbers for each PDF, and specific error handling for validation failures (pausing the current PDF) or stored procedure errors/cost mismatches (offering to skip the PDF or abort the batch). Per-PDF status lines and a final batch summary table are also required.
*   **Session Initialization**: Critical setup tasks are to be performed only once per session. This involves deploying a stored procedure (`ParsecCalculation.sql`) from the project root and caching all 282 `ServiceColumnKeys` into memory for reuse across PDFs.
*   **Performance and Dry Run Features**:
    *   The agent includes a "DRY RUN" mode, triggered by the presence of `DRY_RUN` or `dry_run.txt` files, which skips actual database setup and operations.
    *   It specifies parallel processing for reading all attached PDF files simultaneously and extracting all field values in a single pass to optimize performance.
    *   A mock `insert_template.py` script is defined (to be used if a real one isn't present), simulating database operations with timed delays for benchmarking purposes in dry run mode.
    *   A "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report is generated after processing, detailing timings for PDF reading, extraction, and mock insertion phases.

The content consistently emphasizes robust error handling, strict sequential processing for batches, and efficiency through parallel reading/extraction and a dry-run benchmarking capability.

## 12:49:46 PM
The file `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`, last modified on `6/17/2026, 11:53:22 AM`, contains detailed operational instructions for a "Database Agent" AI. This agent is responsible for processing Parsec print estimate PDFs, extracting job data, and inserting it into a SQL Server database using `pymssql`.

**File-Specific Updates (`db_agent_system_instruction_old.txt`):**

The content outlines a comprehensive workflow with strict rules and protocols:

*   **Core Functionality**: The agent's primary role is to extract data from Parsec PDFs and populate `SQL Server` tables (`PEW.Project`, `PEW.ProjectService`, `PEW.ServiceColumn`) and execute a stored procedure (`ParsecCalculation.sql`).
*   **Absolute Rules**: Strict guidelines are provided, including disallowing `NULL` values (using `''` for text, `'0'` for numeric), mandatory Tier 1 field validation, and ensuring all 282 `ProjectServiceValue` rows are inserted before calling the stored procedure. It also prevents writing emails from config/environment variables to `user_mail.json`.
*   **Multiple PDF Handling (Sequential Batch)**: A detailed process for handling multiple PDF uploads is defined:
    *   Announcing the processing order.
    *   Strictly sequential processing of each PDF (completing all steps for one before starting the next).
    *   Re-querying job numbers (`JN`/`EN`) for each PDF to ensure correct sequencing.
    *   Handling validation errors by pausing the current PDF and awaiting user input.
    *   Managing stored procedure errors or significant cost mismatches (> $0.50) with options to skip the current PDF or abort the entire batch.
    *   Providing per-PDF status lines and a final batch summary table.
*   **Session Initialization**: Tasks performed only once per session for efficiency:
    *   Deploying or updating the `ParsecCalculation.sql` stored procedure from the project root.
    *   Caching all 282 `ServiceColumnKeys` into memory.
*   **DRY RUN Mode**: A significant section details a "DRY RUN" mode activated by the presence of a `DRY_RUN` file or flag. In this mode:
    *   Database setup (SP deployment, key caching) is skipped.
    *   PDFs are read *in parallel* and values are extracted *in one pass*, with explicit timing measurements.
    *   A `mock_insert.py` script (if `insert_template.py` doesn't exist) simulates database interactions and their timings (`time.sleep` with `random.uniform`) without actual DB writes.
    *   A "PARSEC PARALLEL BENCHMARK" report summarizes the timings for reading, extraction, and mock insertion in this mode.

**Patterns and Recurring Elements:**

*   **Emphasis on Automation and Precision**: The instructions repeatedly use phrases like "Follow steps directly and execute immediately," "Never violate," "exactly," and "strictly sequentially," highlighting a design for a highly autonomous and precise agent.
*   **Robust Error Handling and Validation**: Extensive rules for validation, re-validation, and error recovery (pausing, skipping, aborting) are embedded throughout the workflow, particularly for batch processing.
*   **Performance Optimization**: Strategies like "Once Per Session" initialization for stored procedure deployment and key caching, and the explicit parallel processing of PDFs and value extraction in dry run mode, indicate a focus on efficiency.
*   **Simulation and Benchmarking**: The detailed DRY RUN mode with its mock insert script and benchmark report is a strong recurring theme, suggesting a development environment that prioritizes testing, timing, and performance analysis without impacting live databases.
*   **Structured Formatting**: The use of `══════════════════════════════════════════════════════` and `───` dividers helps to clearly delineate different sections and sub-steps of the instructions.
*   **Database Interaction Details**: Specific mentions of `pymssql`, `SQL Server`, table names (`PEW.ServiceColumn`, `PEW.Project`), and a stored procedure (`ParsecCalculation.sql`) indicate a deep integration with a Microsoft SQL Server environment.