# Activity Summary for 6/18/2026

## 10:49:46 AM
The file `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`, last updated on `6/17/2026, 11:53:22 AM`, contains detailed system instructions for a Database Agent.

This agent is tasked with processing Parsec print estimate PDFs, extracting job data, and inserting it into a SQL Server database using `pymssql`. The instructions emphasize direct execution and strict adherence to defined steps, without extended reasoning.

Key updates and rules highlighted in this file include:

*   **Data Integrity and Validation**: Strict rules are in place to prevent `NULL` insertions (requiring `''` for text and `'0'` for numeric), ensure validation of Tier 1 fields, and stop processing to query the user if validation fails. It also mandates inserting all 282 `ProjectServiceValue` rows before calling a stored procedure.
*   **Multiple PDF Handling (Batch Processing)**: Comprehensive guidelines are provided for processing multiple PDFs sequentially in a batch. This includes announcing the processing order, strictly completing all steps for one PDF before moving to the next, re-querying job (JN) and estimate (EN) numbers for each PDF, and specific error handling for validation failures (pausing the current PDF) or stored procedure errors/cost mismatches (offering to skip the PDF or abort the batch). Per-PDF status lines and a final batch summary table are also required.
*   **Session Initialization**: Critical setup tasks are to be performed only once per session. This involves deploying a stored procedure (`ParsecCalculation.sql`) from the project root and caching all 282 `ServiceColumnKeys` into memory for reuse across PDFs.
*   **Performance and Dry Run Features**:
    *   The agent includes a "DRY RUN" mode, triggered by the presence of `DRY_RUN` or `dry_run.txt` files, which skips actual database setup and operations.
    *   It specifies parallel processing for reading all attached PDF files simultaneously and extracting all field values in a single pass to optimize performance.
    *   A mock `insert_template.py` script is defined (to be used if a real one isn't present), simulating database operations with timed delays for benchmarking purposes in dry run mode.
    *   A "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report is generated after processing, detailing timings for PDF reading, extraction, and mock insertion phases.

The content consistently emphasizes robust error handling, strict sequential processing for batches, and efficiency through parallel reading/extraction and a dry-run benchmarking capability.

## 12:49:46 PM
The file `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`, last modified on `6/17/2026, 11:53:22 AM`, contains detailed operational instructions for a "Database Agent" AI. This agent is responsible for processing Parsec print estimate PDFs, extracting job data, and inserting it into a SQL Server database using `pymssql`.

**File-Specific Updates (`db_agent_system_instruction_old.txt`):**

The content outlines a comprehensive workflow with strict rules and protocols:

*   **Core Functionality**: The agent's primary role is to extract data from Parsec PDFs and populate `SQL Server` tables (`PEW.Project`, `PEW.ProjectService`, `PEW.ServiceColumn`) and execute a stored procedure (`ParsecCalculation.sql`).
*   **Absolute Rules**: Strict guidelines are provided, including disallowing `NULL` values (using `''` for text, `'0'` for numeric), mandatory Tier 1 field validation, and ensuring all 282 `ProjectServiceValue` rows are inserted before calling the stored procedure. It also prevents writing emails from config/environment variables to `user_mail.json`.
*   **Multiple PDF Handling (Sequential Batch)**: A detailed process for handling multiple PDF uploads is defined:
    *   Announcing the processing order.
    *   Strictly sequential processing of each PDF (completing all steps for one before starting the next).
    *   Re-querying job numbers (`JN`/`EN`) for each PDF to ensure correct sequencing.
    *   Handling validation errors by pausing the current PDF and awaiting user input.
    *   Managing stored procedure errors or significant cost mismatches (> $0.50) with options to skip the current PDF or abort the entire batch.
    *   Providing per-PDF status lines and a final batch summary table.
*   **Session Initialization**: Tasks performed only once per session for efficiency:
    *   Deploying or updating the `ParsecCalculation.sql` stored procedure from the project root.
    *   Caching all 282 `ServiceColumnKeys` into memory.
*   **DRY RUN Mode**: A significant section details a "DRY RUN" mode activated by the presence of a `DRY_RUN` file or flag. In this mode:
    *   Database setup (SP deployment, key caching) is skipped.
    *   PDFs are read *in parallel* and values are extracted *in one pass*, with explicit timing measurements.
    *   A `mock_insert.py` script (if `insert_template.py` doesn't exist) simulates database interactions and their timings (`time.sleep` with `random.uniform`) without actual DB writes.
    *   A "PARSEC PARALLEL BENCHMARK" report summarizes the timings for reading, extraction, and mock insertion in this mode.

**Patterns and Recurring Elements:**

*   **Emphasis on Automation and Precision**: The instructions repeatedly use phrases like "Follow steps directly and execute immediately," "Never violate," "exactly," and "strictly sequentially," highlighting a design for a highly autonomous and precise agent.
*   **Robust Error Handling and Validation**: Extensive rules for validation, re-validation, and error recovery (pausing, skipping, aborting) are embedded throughout the workflow, particularly for batch processing.
*   **Performance Optimization**: Strategies like "Once Per Session" initialization for stored procedure deployment and key caching, and the explicit parallel processing of PDFs and value extraction in dry run mode, indicate a focus on efficiency.
*   **Simulation and Benchmarking**: The detailed DRY RUN mode with its mock insert script and benchmark report is a strong recurring theme, suggesting a development environment that prioritizes testing, timing, and performance analysis without impacting live databases.
*   **Structured Formatting**: The use of `══════════════════════════════════════════════════════` and `───` dividers helps to clearly delineate different sections and sub-steps of the instructions.
*   **Database Interaction Details**: Specific mentions of `pymssql`, `SQL Server`, table names (`PEW.ServiceColumn`, `PEW.Project`), and a stored procedure (`ParsecCalculation.sql`) indicate a deep integration with a Microsoft SQL Server environment.

## 1:49:46 PM
The provided log entry details changes to the `db_agent_system_instruction_old.txt` file, last modified on **6/17/2026, 11:53:22 AM**. This file outlines the operational guidelines for a Database Agent responsible for processing Parsec print estimate PDFs for Impact Printers & Lithographers and inserting extracted data into a SQL Server database.

Key information from the changes includes:

*   **Core Agent Functionality:** The agent's primary role is to read PDF estimates, extract all job data, and insert it into a SQL Server database using `pymssql` and secure credentials.
*   **Strict Operational Rules:** The instructions emphasize absolute rules for data integrity, such as:
    *   Never inserting NULL values (use `''` for text, `0` for numeric).
    *   Never running stored procedures on invalid or empty Section A values.
    *   Always performing validation, stopping processing, and prompting the user for corrections if Tier 1 fields fail validation.
    *   Mandatory insertion of 282 `ProjectServiceValue` rows before calling the stored procedure.
    *   A critical security rule: never writing emails from config files or environment variables into `user_mail.json`.
*   **Robust Multiple PDF Handling (Batch Processing):**
    *   The system supports processing multiple PDFs sequentially, announcing the queue and processing each PDF fully before moving to the next.
    *   It requires re-querying Job Number (JN) and Estimate Number (EN) for *each* PDF to ensure correct sequential numbering.
    *   Comprehensive error handling is in place for validation failures (pausing the current PDF) and for stored procedure errors or significant cost mismatches (offering options to skip the PDF or abort the entire batch).
    *   Detailed per-PDF status lines and a final batch summary table are generated.
*   **Session-Level Optimizations:**
    *   A stored procedure (`ParsecCalculation.sql`) is deployed or updated only once per session at the start.
    *   All 282 `ServiceColumnKeys` are queried and cached in memory once per session for reuse across all PDFs.
*   **DRY RUN Mode and Performance Benchmarking:**
    *   The system includes a `DRY_RUN` mechanism activated by the presence of specific files (`DRY_RUN`, `dry_run.txt`, `DRY_RUN.txt`). In this mode, database setup (SP deployment, caching keys) is skipped.
    *   It implements parallel PDF reading and a single-pass value extraction across all PDFs, with precise timing recorded for these phases.
    *   If `insert_template.py` does not exist, a mock `insert_template.py` script is generated. This script simulates database insert operations (for EN number, Project, ProjectService, ProjectServiceValue, and the stored procedure call) using `time.sleep` without actually touching the database.
    *   A "PARSEC PARALLEL BENCHMARK" report is generated in DRY RUN mode, detailing timings for PDF reading, extraction, and mock insertion for all processed PDFs.

**Patterns and Recurring Elements:**

The instructions consistently emphasize strict adherence to predefined steps, strong validation, and robust error handling. There's a clear pattern of optimizing performance by executing certain setup tasks only once per session, while ensuring data integrity and sequential consistency for batch operations. The inclusion of a detailed `DRY_RUN` mode with performance benchmarking highlights a focus on testing, development, and efficiency analysis without impacting the live database.

## 2:49:46 PM
The provided log entry details a comprehensive system instruction for a Database Agent tasked with processing Parsec print estimate PDFs for Impact Printers & Lithographers.

**File-specific updates:**

*   **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt` (Timestamp: 6/17/2026, 11:53:22 AM):** This file outlines the exact steps and rules for the Database Agent.
    *   **Core Functionality:** The agent must read PDFs, extract job data, and insert it into a SQL Server database using `pymssql` and secure credentials.
    *   **Strict Rules:** Emphasizes no extended reasoning, direct execution, and immediate action. Critical rules include:
        *   No `NULL` values (use `''` for text, `'0'` for numeric).
        *   Mandatory Tier 1 validation before proceeding; stop and ask the user if validation fails.
        *   All 282 `ProjectServiceValue` rows must be inserted before calling a stored procedure.
        *   Prohibits writing sensitive information (like emails) from config/env files to `user_mail.json`.
    *   **Multiple PDF Handling (Batch Processing - "PRE-STEP B"):**
        *   PDFs are processed strictly sequentially.
        *   Before processing each PDF, the agent must re-query the latest `JN` and `EN` numbers from the database to ensure correct sequencing.
        *   Detailed error handling for validation failures (pause, ask user) and stored procedure errors/cost mismatches (offer "Skip" or "Abort" options).
        *   Requires per-PDF status lines and a final batch summary table upon completion.
    *   **Session Initialization ("PRE-STEP C"):** These tasks are performed only once per session.
        *   **C1: Stored Procedure Deployment:** `ParsecCalculation.sql` is read and executed from the project root to deploy/update the SP. If it fails, processing stops.
        *   **C2: Service Column Key Caching:** All 282 `ServiceColumnKey` values for a specific `ServiceKey` (`4A19C216-1E17-4860-ACC8-6BE1B11DB0AC`) are queried and cached in memory for reuse.
        *   **C3: Insert Template Script & Dry Run:**
            *   A `DRY_RUN` mode is supported (checked by presence of `DRY_RUN` or `dry_run.txt` files), which skips database setup (C1, C2).
            *   PDFs are read *in parallel* in a single tool call batch.
            *   Field values are extracted from *all* PDFs in one pass, generating `values_{EstimateNo}.json` files.
            *   If `insert_template.py` does not exist, a `mock_insert.py` script is written as `insert_template.py`. This mock script simulates database operations with `time.sleep` calls (e.g., simulating EN number query, Project INSERT, ProjectService INSERT, ProjectServiceValue INSERT, and `Update_ParsecCalculation` SP) and provides a detailed dry-run output without touching the actual database.
            *   A "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report is generated after processing all PDFs in dry-run mode, detailing timings for reading, extraction, and mock insertions.

**Timestamps of significant changes:**

*   The primary timestamp is **6/17/2026, 11:53:22 AM**, marking the version or creation of this detailed system instruction. This timestamp is in the future relative to the current date, suggesting it might be a planned or future-dated specification.

**Patterns or recurring elements in the content:**

*   **Emphasis on Automation and Precision:** The instructions are highly prescriptive, emphasizing "follow steps directly," "execute immediately," and "never violate" absolute rules.
*   **Sequential vs. Parallel Processing:** There's a clear pattern of processing PDFs strictly sequentially for overall job completion, but leveraging parallelism for initial steps like PDF reading and data extraction.
*   **Robust Error Handling and Validation:** Recurring themes of validation checks, user interaction for corrections, and defined protocols for handling database errors or data mismatches.
*   **Performance and Optimization:** Instructions to cache data (ServiceColumnKeys) and parallelize initial I/O (PDF reading) suggest an effort to optimize performance. The inclusion of a detailed dry-run benchmarking feature further reinforces this.
*   **Database Interaction Details:** Specific mentions of `pymssql`, `SQL Server`, `PEW.ServiceColumn`, and stored procedures (`ParsecCalculation.sql`) indicate a well-defined database integration strategy.
*   **Structured Step-by-Step Approach:** The use of "PRE-STEP B," "PRE-STEP C," and numbered sub-steps (C1, C2, C3, B1-B7) provides a highly structured and easily auditable execution flow.
*   **Mocking for Development/Testing:** The inclusion of a `DRY_RUN` mode and the `mock_insert.py` script highlights a robust development and testing methodology, allowing for simulation and benchmarking without actual database interaction.