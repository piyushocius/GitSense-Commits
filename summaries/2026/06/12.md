# Activity Summary for 6/12/2026

## 12:34:14 PM
Changes to `d:\Projects\KolossusAI\.env` files are not summarized as per the instruction to exclude files that may store keys.

### File-Specific Updates:

**`d:\Projects\KolossusAI\workforce\email\service.py`** (Last significant change: `6/11/2026, 12:34:14 PM`)
This Python file implements an `EmailService` class for managing email interactions via the Microsoft Graph API. Its core functionalities include:
*   **Token Management**: Dynamically retrieves access tokens for Graph API calls, prioritizing delegated Outlook monitoring tokens, then falling back to regular tenant tokens or main access tokens. It also handles refreshing expired tokens.
*   **Email Sending**: Provides methods to send emails to single or multiple recipients. It supports converting Markdown content to HTML, attaching files, and intelligently handling email threading by attempting to reply to existing conversations if a `message_id` is available from a previous interaction.
*   **Workforce Integration**: Associates emails with specific "Workforce" instances, using the workforce name to construct informative email subjects for better reply routing and user experience in the portal.
*   **Sender Identification**: Determines the sender email based on the tenant configuration.
*   **Error Handling and Logging**: Includes robust logging for various stages of the email sending process, from token acquisition to API call failures.

**`d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`** (Last significant change: `6/12/2026, 11:49:10 AM`)
This text file outlines detailed system instructions for a "Database Agent" tasked with processing Parsec print estimate PDFs and inserting data into a SQL Server database. Key updates and directives include:
*   **Core Mandate**: The agent's primary role is to extract job data from PDFs and insert it into the database using `pymssql`, strictly following defined steps and security rules.
*   **Absolute Rules**: A set of non-negotiable rules, such as preventing NULL insertions, enforcing validation, and ensuring all `ProjectServiceValue` rows are inserted before calling the main stored procedure. A specific update changed the required `ProjectServiceValue` count from `305` to `282`.
*   **Pre-step for Workspace Setup**:
    *   **SP File Check**: Instructions for silently checking for and, if necessary, downloading `ParsecCalculation.sql` from Google Drive for reference, without user notification unless the file is missing.
    *   **Multiple PDF Handling**: Comprehensive guidelines for sequential processing of multiple PDFs in a batch, including queue announcement, per-PDF status reporting, re-querying JN/EN numbers for each PDF, handling validation errors by pausing and prompting the user, and providing a final batch summary.
    *   **Session Initialization (New Addition)**: At `6/12/2026, 11:49:10 AM`, a new pre-step (`PRE-STEP C`) was added to initialize the session once:
        *   **Deploy Stored Procedure**: Specifies reading and executing `ParsecCalculation.sql` to deploy or update the stored procedure in the database at the start of the session.
        *   **Cache ServiceColumnKeys**: Directs the agent to query and cache all 282 `ServiceColumnKey` values for a specific service ID, to be reused across all PDFs in the session.
*   **User Email Acquisition (`STEP 0`)**: Details the process for obtaining the user's email, including checking a `user_mail.json` file, prompting the user if not found, and performing a database lookup to verify the user's account. Strict security rules are enforced regarding email sources (e.g., prohibiting `digudarling@yahoo.com` and preventing storing emails from config files).
*   **Key Lookup (`STEP 1`)**: Instructs the agent to look up the `ClientKey` for "Deutsche Bank" in the database.

### Patterns and Recurring Elements:
*   **Strict Procedural Guidance**: The `db_agent_system_instruction_old.txt` file demonstrates a pattern of highly detailed, step-by-step instructions with explicit "ABSOLUTE RULES" for agent behavior, indicating a need for precise, deterministic execution.
*   **Security Focus**: Across both files, there's an emphasis on security, from handling API keys securely (implied by the `.env` exclusion and token management in `email/service.py`) to explicit rules about handling user emails and credentials in `db_agent_system_instruction_old.txt`.
*   **External Service Integration**: Both files show a clear pattern of integrating with external services: Microsoft Graph API for email, Google Drive for file management, and SQL Server for database operations.
*   **Error Handling and Reporting**: There's a consistent focus on clear error reporting and defined actions to take when errors occur (e.g., stopping immediately, skipping, or aborting batches).
*   **Configuration and Customization**: The presence of an `.env` file (even if not summarized) and various configuration variables in the Python code suggests a highly configurable application.
*   **Placeholder Timestamps**: The consistent use of future dates (e.g., 2026) in the timestamps might indicate these logs are part of a development or testing environment using synthetic time data.

## 2:34:16 PM
The provided log includes changes to two primary files: `d:\Projects\KolossusAI\workforce\email\service.py` and `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`. Changes to `d:\Projects\KolossusAI\.env` are noted but not summarized due to its nature of storing sensitive configuration data and keys.

**File-Specific Updates:**

*   **`d:\Projects\KolossusAI\workforce\email\service.py` (Timestamp: 6/11/2026, 12:34:14 PM)**
    *   This file defines the `EmailService` class, responsible for sending and processing emails using the Microsoft Graph API.
    *   It includes core functionalities for email composition, threading, delivery, and handling incoming emails.
    *   The service incorporates logic to retrieve access tokens, prioritizing a delegated Outlook monitoring token, and falling back to a general tenant or main token if necessary.
    *   It supports converting Markdown content to HTML for email bodies and provides methods for sending emails to single recipients (`send_email`) or multiple recipients (`send_to_multiple`).
    *   A key feature is `send_reply_to_thread`, which enables sending emails as replies to existing conversations by looking up previous `ChannelMessageWorkforce` records.
    *   The service also includes specific handling for "portal-session" clones to ensure email subjects align with the user-facing workforce names in the portal UI, and logic to skip self-notification emails.

*   **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`**
    *   This document outlines the operational instructions for a "Database Agent" for "Impact Printers & Lithographers," primarily focused on extracting data from Parsec print estimate PDFs and inserting it into a SQL Server database.
    *   **Initial Version (Timestamp: 6/12/2026, 11:47:21 AM)**
        *   Establishes strict "ABSOLUTE RULES" including mandates against inserting NULL values, skipping validation, and modifying `ParsecCalculation.sql`.
        *   Details a "PRE-STEP A" for silently checking for and downloading `ParsecCalculation.sql` from Google Drive if it's missing.
        *   Describes "PRE-STEP B" for handling multiple PDFs in sequential batches, including queuing, sequential processing, re-querying job numbers, handling validation errors by pausing, managing stored procedure errors, and generating per-PDF status lines and a final batch summary.
        *   Outlines "STEP 0" for obtaining and validating the user's email, including security rules preventing specific email addresses and requiring explicit user input.
        *   "STEP 1" involves looking up required keys, specifically the `CompanyKey` for "Deutsche Bank."
    *   **Subsequent Versions (Timestamps: 6/12/2026, 11:48:38 AM; 6/12/2026, 11:48:49 AM)**
        *   These entries show no functional changes from the immediate preceding version of the file.
    *   **Latest Version (Timestamp: 6/12/2026, 11:49:10 AM)**
        *   A significant update was made to the "ABSOLUTE RULES," changing the required number of `ProjectServiceValue` rows to insert from **305 to 282**.
        *   A new "PRE-STEP C — SESSION INITIALISATION (ONCE PER SESSION)" section was introduced. This new step mandates:
            *   **C1. Deploying the Stored Procedure:** Executing `ParsecCalculation.sql` once per session to deploy or update the stored procedure in the database, with error handling.
            *   **C2. Caching ServiceColumnKeys:** Querying and caching 282 `ServiceColumnKey` values for a specific service (`4A19C216-1E17-4860-ACC8-6BE1B11DB0AC`) into a dictionary once per session for reuse.

**Patterns and Recurring Elements:**

*   **Strict Operational Guidelines:** The system instruction document uses strong directives ("ABSOLUTE RULES," "NEVER VIOLATE," "EXACTLY") to ensure precise agent behavior, particularly regarding data integrity, validation, and user interaction.
*   **Security and Data Integrity:** Both the email service and the database agent instructions show a strong emphasis on security, from handling authentication tokens and email addresses (`user_mail.json` rules) to strict database insertion policies (no NULLs, validation).
*   **Modular Workflow Definition:** The database agent instructions are meticulously broken down into distinct "PRE-STEP" and "STEP" sections, indicating a structured and sequential workflow for processing tasks.
*   **Integration with External Services:** The project relies on external integrations, including Microsoft Graph API for email, Google Drive for file management, and SQL Server for database operations.
*   **Project Naming:** The consistent presence of "KolossusAI" in file paths and comments confirms the project's identity.
*   **Rapid Iteration:** The numerous, quick changes to the `.env` file (though not summarized) suggest active configuration adjustments. The `db_agent_system_instruction_old.txt` also saw several minor updates in quick succession before a more substantial revision.

## 3:34:16 PM
The changes log details updates across two primary areas: an email service utility and a database agent's system instructions. Notably, all entries for the `.env` file have been omitted from this summary as they contain sensitive information, in accordance with the instructions.

**File-Specific Updates:**

*   **`d:\Projects\KolossusAI\workforce\email\service.py`**
    *   **Timestamp:** 6/11/2026, 12:34:14 PM
    *   **Summary:** This file defines an `EmailService` class responsible for sending emails via the Microsoft Graph API. Key functionalities include sending emails to single or multiple recipients, supporting email threading (replying to existing conversations), handling attachments, and converting Markdown content to HTML. The service is designed to intelligently obtain access tokens (prioritizing delegated Outlook monitoring tokens, then tenant tokens, and finally a main token) and determine the appropriate Graph API endpoint (`/me` vs `/users/{email}`). It also includes logic for identifying and using existing message IDs to send replies within a thread, as well as tracking sent messages. A specific detail is the handling of workforce names in the subject line, especially for portal-session clones, to ensure proper reply routing and user interface consistency.

*   **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`**
    *   **Timestamps:** Multiple updates occurred on 6/12/2026, with the most significant change at 11:49:10 AM.
    *   **Summary:** This file contains a detailed system instruction for a Database Agent tasked with processing Parsec print estimate PDFs and inserting data into a SQL Server database.
        *   **Initial/Minor Changes (until 6/12/2026, 11:48:54 AM):** The core instructions remained consistent, outlining "ABSOLUTE RULES" (e.g., no NULL inserts, strict validation, always insert 305 `ProjectServiceValue` rows before a stored procedure call, no modification of `ParsecCalculation.sql`, strict email handling in `user_mail.json`), "PRE-STEP A" for `ParsecCalculation.sql` file check (with Google Drive integration fallback), "PRE-STEP B" for sequential batch PDF handling (queue announcement, sequential processing, re-querying JN/EN numbers per PDF, specific error handling for validation failures and SP errors, and per-PDF/final batch status reporting), and "STEP 0" for user email verification (checking `user_mail.json`, DB lookup, and strict security rules for email usage). "STEP 1" details looking up a `ClientKey` for 'Deutsche Bank'.
        *   **Significant Change (at 6/12/2026, 11:49:10 AM):**
            *   The number of `ProjectServiceValue` rows required to be inserted was updated from `305` to `282`.
            *   A new section, **"PRE-STEP C — SESSION INITIALISATION (ONCE PER SESSION)"**, was added. This introduces two crucial steps to be run once per session:
                *   **`C1. DEPLOY THE STORED PROCEDURE (ONCE)`:** Instructions for reading `ParsecCalculation.sql`, splitting it into batches, and executing them to deploy or update the stored procedure in the database.
                *   **`C2. CACHE ALL 282 ServiceColumnKeys (ONCE)`:** Instructions to query and cache all `ServiceColumnKey` values from the `PEW.ServiceColumn` table into a dictionary for efficient reuse throughout the session.

**Patterns and Recurring Elements:**

*   **Agent-Centric Design:** Both `service.py` and `db_agent_system_instruction_old.txt` reflect a strong focus on defining precise, step-by-step behaviors and rules for automated agents (e.g., "Database Agent," "Email Service").
*   **Integration with External Systems:** There's a clear pattern of integrating with various external platforms: Microsoft Graph API for email, SQL Server for database operations, and Google Drive for file management.
*   **Emphasis on Data Integrity and Security:** Recurring rules like "Never insert NULL," strict validation, and meticulous handling of user email (e.g., preventing specific emails, only saving explicitly provided emails) highlight a strong concern for data quality and security.
*   **Batch Processing and State Management:** The database agent's instructions detail sequential batch processing of PDFs, requiring re-querying specific numbers for each PDF and caching data (`ServiceColumnKeys`) once per session, demonstrating consideration for performance and correct state management in iterative tasks.
*   **Detailed Logging and Error Handling:** Both components include provisions for logging (`logger.debug`, `logger.error`, `logger.info`) and specific instructions on how to react to errors (e.g., "STOP immediately," "ask the user," "skip this PDF or abort").
*   **Standardized Timestamp Format:** All timestamps use the `M/DD/YYYY, HH:MM:SS AM/PM` format, consistently dated in `2026`.