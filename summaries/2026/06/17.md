# Activity Summary for 6/17/2026

## 12:49:43 PM
The provided log entry from **6/17/2026, 11:53:22 AM** details a system instruction for a "Database Agent" within the `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt` file.

This instruction defines the agent's role as processing Parsec print estimate PDFs to extract job data and insert it into a SQL Server database for Impact Printers & Lithographers.

**Key File-Specific Updates and Instructions:**

*   **Core Operational Rules:** The agent is strictly commanded to avoid extended reasoning, follow steps directly, and execute immediately. It must never insert `NULL` values (using `''` for text and `'0'` for numeric fields instead), validate all Tier 1 fields, and ensure all 282 `ProjectServiceValue` rows are inserted before calling the stored procedure. It's also explicitly forbidden from writing emails from config files or environment variables into `user_mail.json`.
*   **Multiple PDF Handling (Sequential Batch):** The instructions outline a robust process for handling multiple PDFs, emphasizing sequential processing. This includes announcing the processing order, re-querying Job Number (JN) and Estimate Number (EN) for each PDF to ensure correct numbering, pausing a PDF's processing on validation errors, and offering options (skip/abort) if a stored procedure error or significant cost mismatch occurs. It mandates detailed per-PDF status lines and a final batch summary report.
*   **Session Initialization:** Before processing any PDFs, the agent must perform two tasks once per session:
    *   **Deploy Stored Procedure:** It's instructed to read and execute `ParsecCalculation.sql` to deploy or update the stored procedure, stopping if deployment fails.
    *   **Cache ServiceColumnKeys:** All 282 `ServiceColumn` keys related to a specific service (`4A19C216-1E17-4860-ACC8-6BE1B11DB0AC`) are to be queried and cached in memory for reuse across all PDFs.
*   **DRY RUN Mode and Parallel Processing:**
    *   The agent is designed to detect a `DRY_RUN` mode (via `DRY_RUN` file or `dry_run.txt`). If active, it skips actual database setup (SP deployment and key caching).
    *   It's instructed to read all attached PDF files simultaneously using a single tool call batch and extract all field values from these PDFs in one pass, recording timings for both phases.
*   **Mock `insert_template.py` for Benchmarking:**
    *   If `insert_template.py` does not exist, the agent is to generate a mock version. This mock script simulates the entire database insertion process, including queries for EN numbers, and inserts into `PEW.Project`, `PEW.ProjectService`, `ProjectServiceValue` rows, and the `Update_ParsecCalculation` stored procedure, using `time.sleep` to mimic real-world delays.
    *   The mock script outputs a detailed dry-run log for each PDF, showing estimated values, EN, customer, quantity, size, color, price, and the simulated insert duration, explicitly stating "NO DB TOUCHED".
    *   Finally, it generates a "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report summarizing total PDFs processed and phase timings for read, extraction, and mock insert.

**Patterns and Recurring Elements:**

*   **Strict Adherence to Rules:** A strong emphasis on following exact steps, never violating rules, and not engaging in extended reasoning is a recurring theme.
*   **Validation and Error Handling:** Robust validation of Tier 1 fields and comprehensive error handling (skip/abort options, detailed reporting) for both validation failures and database/SP errors are central.
*   **Efficiency and Optimization:** Instructions for caching data (`ServiceColumnKeys`) and performing parallel operations (PDF reading, value extraction) highlight an emphasis on performance.
*   **Sequential vs. Parallel Operations:** While PDF reading and extraction are parallelized, core PDF processing (steps 0-8) and batch handling are strictly sequential, demonstrating a thoughtful design for concurrency where appropriate.
*   **Dry Run Capability:** The inclusion of a detailed dry-run mode with a mock database script indicates a focus on testing, benchmarking, and development without impacting live data.
*   **Detailed Logging and Reporting:** Instructions for announcing queues, per-PDF status lines, and comprehensive summary reports are prevalent, ensuring transparency and trackability of operations.

## 1:49:45 PM
The provided log details changes to `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt` on **6/17/2026, 11:53:22 AM**.

This file outlines the comprehensive system instructions for a "Database Agent" designed for Impact Printers & Lithographers. Its primary role is to process Parsec print estimate PDFs, extract all job data, and insert it into an SQL Server database using `pymssql`.

**File-Specific Updates (`db_agent_system_instruction_old.txt`):**

The document defines a highly structured workflow with several key components:

1.  **Absolute Rules:**
    *   Strict guidelines for database operations, including never inserting `NULL` (using `''` for text and `'0'` for numeric values), mandatory validation before running stored procedures, and ensuring all 282 `ProjectServiceValue` rows are inserted before calling the stored procedure.
    *   Prohibitions against skipping validation or writing emails from config/environment variables to `user_mail.json`.

2.  **Multiple PDF Handling (Sequential Batch):**
    *   Details how to process multiple PDFs uploaded in a single session. This includes announcing the processing order, completing all steps for one PDF before moving to the next, re-querying Job Number (JN)/Estimate Number (EN) for each PDF to ensure sequential numbering, and specific error handling for validation failures (pausing and asking the user) or stored procedure errors/cost mismatches (offering to skip or abort the batch).
    *   Requires printing a one-line status after each PDF and a comprehensive summary table upon batch completion.

3.  **Session Initialization (Once Per Session):**
    *   **Stored Procedure Deployment:** Dictates reading `ParsecCalculation.sql` and deploying/updating the stored procedure in the database only once at the beginning of the session.
    *   **ServiceColumnKeys Caching:** Requires querying and caching 282 `ServiceColumn` keys into memory once per session for reuse.

4.  **DRY RUN Mode and Parallel Processing Benchmarking:**
    *   A significant section details a "DRY RUN" mode, activated by the presence of a `DRY_RUN` file. In this mode, all actual database setup (SP deployment, key caching) is skipped.
    *   It introduces **parallel PDF reading** (all PDFs read simultaneously in a single tool call batch) and **parallel value extraction** (all values extracted in one pass). Timings for these phases are recorded.
    *   If `insert_template.py` does not exist, a `mock_insert.py` script is generated. This mock script simulates the full insertion process (EN number query, `PEW.Project`, `PEW.ProjectService`, `PEW.ProjectServiceValue` inserts, and `Update_ParsecCalculation` stored procedure call) using random `time.sleep` calls to mimic database latency, without actually touching the database.
    *   After processing (or simulating processing) all PDFs, a "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report is generated, detailing the number of PDFs processed and phase timings for reading, extraction, and mock insertion.

**Patterns and Recurring Elements:**

*   **Emphasis on Strictness:** The document repeatedly uses terms like "IMPORTANT," "ABSOLUTE RULES — NEVER VIOLATE," "follow steps directly and execute immediately," and "exactly," highlighting a demand for highly deterministic and error-resistant execution.
*   **Sequential vs. Parallel Processing:** While the overall processing of PDF *batches* for insertion is strictly sequential (one PDF fully completed before the next), the initial phases of PDF *reading* and *value extraction* are explicitly designed for parallel execution to optimize performance, especially in the DRY RUN mode.
*   **Clear State Management:** Instructions differentiate between tasks to be performed "ONCE PER SESSION" versus "PER PDF," ensuring efficient resource utilization (e.g., caching service keys, deploying SPs).
*   **Robust Error Handling and User Interaction:** Detailed steps are provided for handling validation failures, database errors, and price discrepancies, often requiring user input or offering clear choices (skip/abort).
*   **Performance Measurement:** The DRY RUN mode's inclusion of detailed timing reports for different processing phases (`read_duration`, `extract_duration`, `insert_duration`) indicates a focus on understanding and optimizing the agent's performance.

## 2:49:48 PM
The `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt` file, last modified on June 17, 2026, at 11:53:22 AM, outlines comprehensive instructions for a Database Agent responsible for processing Parsec print estimate PDFs.

Key instructions for the agent include:
*   **Direct Execution:** The agent is mandated to follow steps directly, execute immediately, and avoid extended reasoning or thinking before acting.
*   **Database Interaction:** It uses `pymssql` with secure credentials to insert job data into a SQL Server database, specifically requiring the insertion of 282 `ProjectServiceValue` rows before calling a stored procedure.
*   **Data Integrity and Validation:** Strict rules prohibit inserting `NULL` values (using `''` for text, `'0'` for numeric), running stored procedures on invalid data, and skipping validation. If a Tier 1 field fails validation, processing stops, and the user must be prompted for correction, with re-validation occurring afterwards.
*   **Security/Privacy:** An explicit rule forbids writing emails from config files or environment variables into `user_mail.json`.

**Handling Multiple PDFs (Batch Processing):**
The instructions detail a robust sequential batch processing mechanism:
*   **Queue Announcement:** If multiple PDFs are uploaded, the agent must announce the processing order.
*   **Strict Sequential Processing:** Each PDF must be fully processed (steps 0-8) before moving to the next; parallel processing of steps between PDFs is disallowed.
*   **Dynamic Numbering:** `JN/EN` numbers must be re-queried from the database for each PDF, ensuring sequential numbering even if prior inserts failed, rather than being cached.
*   **Error Handling:** For validation errors, the specific PDF in question is paused, awaiting user input, while other PDFs are not started. If a stored procedure error or significant cost mismatch (>$0.50) occurs, the user is given options to either skip the current PDF and continue the batch or abort the entire batch.
*   **Status Reporting:** A per-PDF status line is required after each PDF's completion, and a comprehensive summary table is presented at the end of the batch.

**Session Initialization (Once per Session):**
Before processing any PDFs, two critical setup tasks are performed once per session:
*   **Stored Procedure Deployment:** The `ParsecCalculation.sql` script is read and executed to deploy or update the stored procedure in the database. Failure halts the process.
*   **ServiceColumnKeys Caching:** All 282 `ServiceColumn` keys related to a specific `ServiceKey` are queried and cached in memory, to be reused across all PDFs without re-querying.

**Dry Run Mode and Performance Benchmarking:**
The instructions introduce a `DRY_RUN` mode, activated by the presence of specific files or environment variables. In this mode:
*   Database setup (SP deployment, key caching) is skipped.
*   Emphasis is placed on **parallel execution** for initial steps: all attached PDFs are read simultaneously in a single tool call, and all field values are extracted from all PDFs in one pass.
*   If `insert_template.py` does not exist, a mock `insert_template.py` script is generated. This script simulates various database insertion and stored procedure execution times using random delays, printing a `[DRY RUN]` report without touching the actual database.
*   Finally, a "PARSEC PARALLEL BENCHMARK" report is generated, detailing the timings for parallel PDF reading, extraction, and mock insertions, highlighting performance aspects of the process.

**Patterns and Recurring Elements:**
A clear pattern is the emphasis on **efficiency through parallelization** for initial reading and extraction phases, contrasted with **strict sequential processing** for individual PDF data processing and database interaction steps. There's also a consistent focus on **robust error handling, user feedback, and detailed logging/reporting** at various stages of the process, along with a strong commitment to data integrity and a "fail-fast" approach when critical validation or setup steps fail. The use of a `DRY_RUN` mode with mock operations underscores a development and testing methodology focused on performance and reliability.

## 3:49:46 PM
The provided log details changes to a system instruction file.

**File:** `d:\Projects\KolossusAI\scripts\db_agent_system_instruction_old.txt`
**Timestamp:** 6/17/2026, 11:53:22 AM

This file contains comprehensive instructions for a "Database Agent" designed for "Impact Printers & Lithographers." The agent's primary role is to process Parsec print estimate PDFs, extract job data, and insert it into a SQL Server database using `pymssql`.

Key aspects of the instructions include:

*   **Strict Adherence:** Emphasizes direct execution, immediate action, and never violating "ABSOLUTE RULES."
*   **Database Integrity:** Mandates never inserting `NULL` (using `''` for text, `'0'` for numeric), strict validation of Tier 1 fields before proceeding, and inserting all 282 `ProjectServiceValue` rows prior to calling the stored procedure.
*   **Multiple PDF Handling (Sequential Batch):**
    *   Requires announcing the queue and processing PDFs strictly one by one, completing all steps for a single PDF before moving to the next.
    *   `JN` and `EN` numbers must be re-queried for each PDF, not cached.
    *   Detailed error handling for validation failures (pause, ask user, re-validate) and stored procedure errors/cost mismatches (report issue, offer skip/abort options).
    *   Per-PDF status lines and a final batch summary table are required.
*   **Session Initialization (Once Per Session):**
    *   **Stored Procedure Deployment:** `ParsecCalculation.sql` must be read and executed once at the start of the session to deploy/update the SP. If this fails, the process stops.
    *   **ServiceColumnKeys Caching:** All 282 `ServiceColumnKey` values from `PEW.ServiceColumn` must be queried and cached in memory once for the entire session.
    *   **Insert Template Script Building:**
        *   Checks for a `DRY_RUN` flag (file or environment variable). If active, it skips actual DB setup.
        *   **Parallel Processing for Initial Steps:** Explicitly instructs to read *all* attached PDF files simultaneously in a single tool call batch and extract all field values from *all* PDFs in one pass.
        *   If `insert_template.py` does not exist, it writes a `mock_insert.py` script to simulate database operations (e.g., EN number query, various `INSERT` statements, SP call) using `time.sleep` without actually touching the database.
        *   After running the (mock) insert template for all PDFs, it outputs a "PARSEC PARALLEL BENCHMARK — DRY RUN RESULTS" report with phase timings.

**Patterns and Recurring Elements:**

*   **Emphasis on robustness and error handling:** Extensive rules cover validation, data integrity, and how to react to various failures, often involving user interaction.
*   **Clear separation of sequential vs. parallel processing:** Initial PDF reading and data extraction are optimized for parallel execution, while subsequent database operations and processing of individual PDFs are strictly sequential.
*   **Performance optimization:** Caching and one-time setup (SP deployment) are highlighted to reduce overhead.
*   **DRY RUN capability:** A well-defined mechanism is in place for testing and benchmarking the entire process without real database modifications, providing simulated timings.