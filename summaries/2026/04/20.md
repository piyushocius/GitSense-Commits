# Activity Summary for 4/20/2026

## 1:45:23 AM
The `d:\Projects\KolossusAI\tools\schemas.py` file, updated around 4/16/2026, 5:08:51 PM and 5:08:57 PM, serves as the unified registry for all KolossusAI tool definitions. Each tool is consistently structured with an OpenAI-compatible `schema`, a detailed `guide` tailored for Claude instructions, and `metadata`.

Key tool definitions include:

*   **`knowledge_base_search`**: This tool allows for semantic and keyword hybrid searching of documents attached to a workforce. Its schema defines parameters like `query` (required), `limit` (1-20 results), and `min_score` (relevance threshold). The guide emphasizes always searching first before stating lack of information, citing sources, and distinguishes between unstructured (vector DB) and structured (raw files like CSVs) knowledge bases. It provides a `curl` example for execution and details important usage tips. Metadata classifies it under `knowledge_base` with an `availability_rule` of `has_kb`.
*   **`send_email`**: This tool facilitates sending emails with support for multiple `to`, `cc`, and `bcc` recipients, a `subject`, a `body` (supporting markdown), and file `attachments` (encoded in Base64). The guide includes `curl` examples for simple emails and those with attachments, clarifying that emails are sent from a service account and require no user confirmation. Metadata categorizes it under `communication` with an `availability_rule` of `always`.

The `d:\Projects\KolossusAI\tools\__init__.py` file, updated at 4/16/2026, 6:31:30 PM, is responsible for the dynamic loading and registration of these tools. It provides core classes (`Tool`, `ToolMetadata`, etc.), a `ToolRegistry` for managing tools, and `AvailabilityRuleEngine` for filtering. The file outlines a clear process for adding new tools, requiring definitions in `schemas.py` and tool classes using an `@register_tool` decorator. A significant part of this file is the `_auto_import_tools()` function, which dynamically imports specific tool modules such as `tools.kb`, `tools.email`, `tools.apify`, `tools.image`, `tools.browser`, and `tools.memory`. This ensures that their `@register_tool` decorators are triggered, making the tools available within the KolossusAI framework.

**Patterns and Recurring Elements:**

*   **Standardized Tool Structure:** All tools adhere to a `schema`, `guide`, `metadata` dictionary format for consistent definition.
*   **Detailed Guides:** Each tool's `guide` consistently includes sections like "WHEN TO USE", "HOW TO CALL" (often with Bash `curl` examples interacting with `KOLOSSUS_API_URL` and `KOLOSSUS_TOKEN`), "Parameters", "RESPONSE FORMAT", and "IMPORTANT" notes.
*   **Metadata Consistency:** Tool `metadata` consistently defines `version`, `category`, `availability_rule`, `is_builtin`, `rate_limit_per_minute`, `cache_ttl_seconds`, `timeout_ms`, `api_endpoint`, and `tags`.
*   **Dynamic Tool Registration:** The system relies on a dynamic registration mechanism (`_auto_import_tools` and `@register_tool`) to make new tools discoverable and usable.
*   **API-driven Execution:** Tools are designed to be executed via HTTP POST requests to specified API endpoints, ensuring a unified interaction model.

## 2:45:26 AM
The `d:\Projects\KolossusAI\tools\schemas.py` file, with timestamps around 4/16/2026, 5:08:51 PM and 5:08:57 PM (indicating no functional change within that short interval), serves as the unified registry for KolossusAI's tool definitions. It consistently structures each tool with a `schema` (in OpenAI function format), a detailed `guide` (for Claude instructions), and `metadata`.

Two key tools are defined:

1.  **`knowledge_base_search`**: This tool is designed for searching knowledge bases, supporting both unstructured documents (indexed in a vector DB) and structured files (read directly from disk if paths are provided).
    *   Its schema includes parameters for `query`, `limit` (1-20, default 5), and `min_score` (0.0-1.0, default 0.5), with `query` being required.
    *   The guide provides mandatory usage scenarios (e.g., user asks about documentation, policies), a Bash `curl` example for execution, and critical instructions to *always search first* before claiming lack of information and to *always cite sources*.
    *   Metadata categorizes it under `knowledge_base`, with an `availability_rule` of `has_kb`, a rate limit of 30 per minute, and a cache TTL of 300 seconds.

2.  **`send_email`**: This tool enables sending emails with support for file attachments.
    *   Its schema requires `to`, `subject`, and `body`, with optional `cc`, `bcc`, and `attachments`. Attachments are defined by `name`, `content_base64`, and `content_type`.
    *   The guide specifies when to use the tool (e.g., user asks to send an email or file), provides Bash `curl` examples for both simple emails and those with attachments (demonstrating base64 encoding for content). It notes that the email is sent from a service account, the body supports markdown, and no user confirmation should be requested.
    *   Metadata places it in the `communication` category, with an `availability_rule` of `always`, a rate limit of 10 per minute, and no caching.

The `d:\Projects\KolossusAI\tools\__init__.py` file, timestamped 4/16/2026, 6:31:30 PM, establishes the core of the dynamic, OOP-based tool framework. It imports base classes, tool registry components, and availability rules. A significant aspect is the `_auto_import_tools()` function, which dynamically imports specific tool modules like `tools.kb`, `tools.email`, `tools.apify`, `tools.image`, `tools.browser`, and `tools.memory`. This function handles potential import errors with logging, indicating a robust setup for managing tool dependencies. The design suggests new tools can be easily integrated by adding their definitions to `schemas.py` and creating a corresponding tool class that gets automatically registered.

**Patterns and Recurring Elements:**

*   **Unified Tool Definitions:** A consistent structure is enforced for all tool definitions in `schemas.py`, including `schema`, `guide`, and `metadata`.
*   **Detailed Documentation:** Every tool is accompanied by an extensive "guide" section, detailing usage, when to invoke, `curl` examples, parameters, response formats, and important considerations, emphasizing clarity for both human and AI agents.
*   **API-Centric Execution:** Tool execution examples consistently use `curl` commands to interact with a standardized API endpoint (`${KOLOSSUS_API_URL}/api/v1/tools/<tool_name>/execute/`), secured by an `Authorization` header with a `Bearer ${KOLOSSUS_TOKEN}`, indicating a uniform API gateway for all tools.
*   **Rich Metadata:** Each tool's `metadata` provides granular control over its behavior, including `version`, `category`, `availability_rule`, `rate_limit_per_minute`, `cache_ttl_seconds`, `timeout_ms`, `api_endpoint`, and `tags`.
*   **Dynamic Tool Registration and Modularity:** The framework supports dynamic discovery and registration of tools through the `_auto_import_tools()` function and the `@register_tool` decorator, promoting an extensible and modular design for the tool ecosystem, with placeholders for future tools.