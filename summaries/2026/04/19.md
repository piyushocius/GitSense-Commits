# Activity Summary for 4/19/2026

## 12:45:23 PM
The `d:\Projects\KolossusAI\tools\schemas.py` file, updated on **4/16/2026, 5:08:51 PM** and saved again at **5:08:57 PM**, serves as a central registry (`TOOLS`) for KolossusAI's unified tool definitions. Each tool adheres to a consistent structure, encompassing an OpenAI-formatted `schema`, a detailed `guide` (likely for LLMs), and `metadata`. The file includes definitions for two primary tools:

*   **`knowledge_base_search`**: This tool is designed for semantic and keyword hybrid search across knowledge bases. Its schema defines a `query` (required), `limit` (1-20 results), and `min_score` (0.0-1.0). The guide provides extensive instructions on when to use it (e.g., for documentation, policies, factual info), emphasizing searching structured (`.csv`, `.xlsx`) and unstructured (PDF, DOCX) documents. It includes `curl` examples demonstrating how to execute the tool via a `KOLOSSUS_API_URL` with `KOLOSSUS_TOKEN` for authorization. Important notes include citing sources and always searching before declaring no information. Its metadata indicates it's a `knowledge_base` category tool with a `has_kb` availability rule, a rate limit of 30 per minute, and a cache TTL of 300 seconds.
*   **`send_email`**: This tool facilitates sending emails with support for file attachments. Its schema includes `to` (required), `subject` (required), `body` (required, supporting markdown), optional `cc`, `bcc`, and `attachments` (an array of objects with `name`, `content_base64`, `content_type`). The guide details its use cases (e.g., notifying users, emailing reports) and provides `curl` examples for simple emails and emails with base64-encoded attachments. It highlights that emails are sent from a service account and that markdown in the body is converted to HTML. Its metadata places it in the `communication` category with an `always` availability rule, a rate limit of 10 per minute, and no caching.

The `d:\Projects\KolossusAI\tools\__init__.py` file, updated on **4/16/2026, 6:31:30 PM**, initializes the `tools` package. This file imports essential components like base tool classes (`Tool`, `ToolMetadata`), a `ToolRegistry`, and `AvailabilityRule` definitions. It explicitly exports the `TOOLS` registry from `schemas.py`. A key feature is the `_auto_import_tools()` function, which dynamically imports various tool modules (e.g., `tools.kb`, `tools.email`, `tools.apify`, `tools.image`, `tools.browser`, `tools.memory`) to trigger their `@register_tool` decorators, ensuring new tools are automatically discovered and made available. This module also includes commented-out entries for future tools, indicating planned expansion.

**Recurring elements and patterns across these changes:**

*   **Unified Tool Framework:** There's a strong emphasis on a unified and extensible tool framework, where tools are defined centrally in `schemas.py` and dynamically loaded.
*   **Structured Definitions:** Each tool follows a precise structure (schema, guide, metadata), ensuring consistency and clear documentation for both LLMs and developers.
*   **API-First Approach:** Tool execution is consistently demonstrated via `curl` commands interacting with a RESTful API (`KOLOSSUS_API_URL`), relying on environment variables for authorization (`KOLOSSUS_TOKEN`).
*   **LLM-Oriented Guides:** The `guide` sections are verbose and written in Markdown, suggesting they are designed to be easily digestible by large language models for tool selection and usage instructions.
*   **Metadata for Control:** Metadata is consistently used to define operational aspects like versioning, category, availability rules, rate limits, caching behavior, and API endpoints, providing granular control over tool deployment and usage.
*   **Dynamic Registration:** The `@register_tool` decorator and the `_auto_import_tools` function in `__init__.py` illustrate a pattern for automatic tool discovery and integration, promoting modularity.

## 1:45:22 PM
The `d:\Projects\KolossusAI\tools\schemas.py` file, last significantly updated on **4/16/2026 at 5:08:51 PM**, defines the unified schema for AI tools within KolossusAI. Each tool is structured with an OpenAI-compatible `schema`, a detailed `guide` (tailored for Claude instructions), and `metadata`. This file details two key tools:

1.  **`knowledge_base_search`**: This tool is designed for semantic and keyword hybrid search within attached knowledge bases. Its schema requires a `query` and optionally accepts `limit` (1-20 results, default 5) and `min_score` (0.0-1.0, default 0.5). The guide specifies its use for documentation, policies, and factual information, emphasizing that it should "ALWAYS search FIRST" and cite sources. It distinguishes between unstructured (vector DB) and structured (`.csv`, `.xlsx`, etc.) knowledge bases, providing `curl` examples for invocation and detailing the expected JSON response format. Metadata includes a `knowledge_base` category, `has_kb` availability rule, and a rate limit of 30 requests per minute.
2.  **`send_email`**: This tool allows sending emails to one or more recipients, supporting attachments. Its schema requires `to`, `subject`, and `body` fields, with optional `cc`, `bcc`, and `attachments` (which expect base64 encoded content). The guide provides clear instructions for when to use it (e.g., user asks to email information), includes simple and attachment-inclusive `curl` examples, and outlines the JSON response. Important notes specify that emails are sent from a service account, the body supports markdown, and no user confirmation should be sought. Metadata categorizes it under `communication`, with an `always` availability rule, and a rate limit of 10 requests per minute.

The `d:\Projects\KolossusAI\tools\__init__.py` file, updated on **4/16/2026 at 6:31:30 PM**, sets up the dynamic tool framework. It imports base classes, registry functions (`register_tool`, `get_tool`, `get_tools_for_workforce`), and availability rule components. The file's docstring outlines the four-step process for adding new tools, which includes defining them in `schemas.py`, creating a tool class with `@register_tool`, and importing the module. A crucial `_auto_import_tools` function dynamically imports specific tool categories (`tools.kb`, `tools.email`, `tools.apify`, `tools.image`, `tools.browser`, `tools.memory`) to trigger their registration, with commented-out entries for future tools like `document_generator` and `web`.

**Patterns and Recurring Elements:**

*   **Unified Tool Definition:** A consistent structure for tools, including `schema`, `guide`, and `metadata`, is enforced across all tool definitions.
*   **LLM-Centric Guides:** Each tool features a comprehensive `guide` section, tailored for LLM agents, detailing "WHEN TO USE," "HOW TO CALL" with `curl` examples, "PARAMETERS," "RESPONSE FORMAT," and "IMPORTANT" considerations.
*   **API Interaction via `curl`:** Tools are designed to be executed via `POST` requests to a standardized API endpoint (`/api/v1/tools/<tool_name>/execute/`), using `Authorization` and `Content-Type` headers, with examples provided in Bash.
*   **Dynamic Registration:** The system relies on `@register_tool` decorators and a central `_auto_import_tools` function in `__init__.py` for automatic tool discovery and integration.
*   **Standardized Metadata:** Tool metadata consistently includes `version`, `category`, `availability_rule`, `is_builtin`, `rate_limit_per_minute`, `cache_ttl_seconds`, `timeout_ms`, `api_endpoint`, and `tags`, providing uniform operational context.