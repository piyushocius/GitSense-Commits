# Activity Summary for 4/21/2026

## 1:45:27 AM
The `d:\Projects\KolossusAI\tools\schemas.py` file serves as the unified registry for all AI tools within the KolossusAI system, defining their OpenAI-compatible schemas, detailed Claude instructions (guides), and operational metadata. Two entries are present:

*   **`knowledge_base_search`**:
    *   **Description**: Facilitates semantic and keyword hybrid search across knowledge bases, explicitly requiring searches before stating lack of information and mandating source citation.
    *   **Parameters**: Includes `query` (string, required), `limit` (integer, 1-20, default 5), and `min_score` (number, 0.0-1.0, default 0.5).
    *   **Guide**: Provides comprehensive instructions on when to use (e.g., user asks about documentation, policies), how to call via a `curl` POST request to `${KOLOSSUS_API_URL}/api/v1/tools/knowledge_base_search/execute/` with `KOLOSSUS_TOKEN` authorization, response format, and search tips. It distinguishes between unstructured and structured knowledge bases.
    *   **Metadata**: Version 1.0.0, categorized under `knowledge_base`, available when a KB exists (`availability_rule: "has_kb"`), with a rate limit of 30 per minute and a 300-second cache TTL.
    *   **Timestamp**: This file was recorded at 4/16/2026, 5:08:51 PM and again at 4/16/2026, 5:08:57 PM. The content between these two timestamps is identical, suggesting no functional changes were made in this very short interval.

*   **`send_email`**:
    *   **Description**: Enables sending emails to multiple recipients, supporting file attachments.
    *   **Parameters**: Requires `to`, `subject`, `body` (supports markdown), and optionally accepts `cc`, `bcc`, and `attachments` (array of objects with `name`, `content_base64`, `content_type`).
    *   **Guide**: Details its use cases (e.g., user asks to send an email or notify someone), how to invoke it using `curl` to `${KOLOSSUS_API_URL}/api/v1/tools/send_email/execute/` with examples for simple and attachment-inclusive emails. Important notes include sending from a service account and not asking for user confirmation.
    *   **Metadata**: Version 1.0.0, categorized under `communication`, always available (`availability_rule: "always"`), with a rate limit of 10 per minute and no caching.

The `d:\Projects\KolossusAI\tools\__init__.py` file, updated at 4/16/2026, 6:31:30 PM, acts as the entry point for the KolossusAI tools package. It sets up the core framework for dynamic, OOP-based tool integration.

*   It imports essential base classes like `Tool`, `ToolMetadata`, `ToolExecutionContext`, and related enums for tool definition and execution.
*   It establishes the `ToolRegistry` and related functions (`register_tool`, `get_registry`, `get_tool`, `get_all_tools`, `get_tools_for_workforce`) for managing and accessing tools.
*   `AvailabilityRule` and `AvailabilityRuleEngine` are imported for controlling tool visibility based on specific conditions.
*   Crucially, it includes an `_auto_import_tools()` function that dynamically imports specific tool modules (e.g., `tools.kb`, `tools.email`, `tools.apify`, `tools.image`, `tools.browser`, `tools.memory`), triggering their `@register_tool` decorators. This mechanism ensures tools are automatically registered and discoverable when the package is loaded. The presence of commented-out modules (`tools.document_generator`, `tools.web`) indicates planned future extensions.
*   The `__all__` list exports key classes and functions, making them easily accessible when importing from the `tools` package.

**Patterns and Recurring Elements:**

*   **Standardized Tool Definition**: All tools adhere to a consistent structure in `schemas.py`, including `schema`, `guide`, and `metadata`, promoting uniformity and ease of integration.
*   **API-Driven Execution**: Both showcased tools (`knowledge_base_search` and `send_email`) are designed to be executed via POST requests to a centralized `KOLOSSUS_API_URL` endpoint using `curl`, implying a standardized API gateway for tool interaction.
*   **Dynamic Tool Management**: The `__init__.py` file, along with the `_auto_import_tools()` function and `@register_tool` decorators, demonstrates a system for dynamic discovery, loading, and registration of tools.
*   **Comprehensive Documentation within Code**: The "guide" section for each tool, coupled with extensive docstrings and comments, provides clear instructions for developers and LLMs on tool usage, parameters, and expected behaviors.
*   **Modular Architecture**: The `_auto_import_tools()` function imports tools from separate modules (`tools.kb`, `tools.email`, etc.), indicating a modular design where each tool category can be developed and managed independently.
*   **Metadata for Control**: Each tool's `metadata` includes critical operational details such as `availability_rule`, `rate_limit_per_minute`, `cache_ttl_seconds`, and `timeout_ms`, enabling fine-grained control over tool behavior and resource usage.
*   **Future Scalability**: The commented-out modules in `__init__.py` hint at an architecture designed for easy expansion with new tool categories.