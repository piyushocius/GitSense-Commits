# Activity Summary for 4/18/2026

## 3:45:21 AM
The `d:\Projects\KolossusAI\tools\schemas.py` file, observed at 4/16/2026, 5:08:51 PM and again at 4/16/2026, 5:08:57 PM (with identical content across both timestamps), serves as the unified registry for all tool definitions within KolossusAI. It uses a `TOOLS` dictionary where each entry defines a tool with its OpenAI-format `schema`, a detailed `guide` (formatted for Claude instructions), and `metadata`.

Key updates within `schemas.py` include:
*   **`knowledge_base_search` tool:** This tool is designed for semantic and keyword hybrid search across knowledge bases. Its `schema` defines `query` as a required parameter, with optional `limit` (1-20 results) and `min_score` (0.0-1.0 relevance). The `guide` provides mandatory use cases (e.g., user asks about documentation, policies), detailed `curl` examples for execution, and specifies that structured knowledge bases (CSV, TSV, Excel files) should be read directly if paths are available in session context, otherwise relying solely on `knowledge_base_search`. It emphasizes citing sources and searching first before declaring lack of information. Metadata indicates it's a `knowledge_base` category tool, available when `has_kb`, with rate limiting and caching.
*   **`send_email` tool:** This tool facilitates sending emails via Microsoft Graph API, supporting multiple recipients, subject, body (markdown), CC, BCC, and file attachments. Its `schema` requires `to`, `subject`, and `body`, while `cc`, `bcc`, and `attachments` are optional. Attachments require `name`, `content_base64`, and `content_type`. The `guide` provides use cases (e.g., user asks to send an email, notify), `curl` examples for simple emails and emails with attachments (showing base64 encoding), and important notes about sender, markdown support, and not asking for user confirmation. Metadata categorizes it as `communication`, always available, with rate limiting but no caching (`cache_ttl_seconds: 0`).

The `d:\Projects\KolossusAI\tools\__init__.py` file, last changed at 4/16/2026, 6:31:30 PM, initializes the tools package. It sets up a dynamic, OOP-based framework for LLM integration. This file imports core classes (`Tool`, `ToolMetadata`, etc.), the `ToolRegistry` and related functions (`register_tool`, `get_tool`, `get_tools_for_workforce`), `AvailabilityRule` components, and the `TOOLS` dictionary from `schemas.py`.

Significant changes and patterns in `__init__.py`:
*   It outlines a structured process for adding new tools, requiring definition in `schemas.py`, creation of a tool class with an `@register_tool` decorator, and module import.
*   A private function `_auto_import_tools()` is responsible for dynamically importing tool modules to ensure registration. The current list of imported modules includes `'tools.kb'`, `'tools.email'`, `'tools.apify'`, `'tools.image'`, `'tools.browser'`, and `'tools.memory'`.
*   Commented-out entries for `'tools.document_generator'` and `'tools.web'` within `_auto_import_tools()` suggest planned future tool integrations.
*   The file includes basic logging for successful or failed module imports during the auto-import process.

**Recurring elements and patterns observed across the changes:**
*   **Unified Tool Structure:** All tools are consistently defined in `schemas.py` with `schema`, `guide`, and `metadata` components.
*   **Standardized API Interaction:** Tool guides consistently instruct agents to use the Bash tool with `curl` to make POST requests to `/api/v1/tools/<tool_name>/execute/` endpoints, using `KOLOSSUS_API_URL` and `KOLOSSUS_TOKEN` environment variables.
*   **Detailed Guidance:** Each tool's `guide` includes "WHEN TO USE", "HOW TO CALL" (with examples), "Parameters", "RESPONSE FORMAT", and "IMPORTANT" sections, providing comprehensive instructions for LLM interaction.
*   **Metadata Consistency:** Metadata for each tool includes standard fields like `version`, `category`, `availability_rule`, `is_builtin`, rate limits, cache settings, API endpoints, and descriptive tags.
*   **Dynamic Registration:** The system employs a dynamic registration mechanism (`@register_tool` decorator and `_auto_import_tools()` in `__init__.py`) to automatically discover and integrate new tools into the framework.