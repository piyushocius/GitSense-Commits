# Activity Summary for 5/22/2026

## 12:54:31 PM
The provided log details changes across several Python files and one text file within a "KolossusAI" project. No summary will be generated for filepaths containing "env" as per the instruction.

### File-specific Updates:

**1. `d:\Projects\KolossusAI\orchestrator\worker.py`**
*   **Timestamp:** 5/18/2026, 6:29:37 PM, 5/18/2026, 6:30:43 PM, 5/18/2026, 7:10:06 PM, 5/19/2026, 12:55:14 PM
*   **Key Changes:** This file appears to be a core component of the orchestrator, importing a vast array of modules related to agent management, memory, planning, prompt building, retrieval, Redis utilities, response generation, summarization, tool execution, usage tracking, logging, vector storage, fact checking, and guardrails. It also includes dynamic imports for PII protection and multi-agent routing.
    *   **Initial state (6:29 PM):** The file defines several helper functions:
        *   `create_tool_turn_safe`: Creates an `OrchTurn` with row locking for concurrency safety.
        *   `_sanitize_text`: Trims and limits text length.
        *   `_get_model_pricing`: Retrieves AI model pricing from the database with fallback mechanisms.
        *   `_clean_file_links_in_response`: Cleans markdown file links in LLM responses, either removing them if files are attached or replacing them with download URLs.
        *   `_normalize_chart_output`: Normalizes various chart output formats into a standard ````chart {json}```` block.
    *   **Subsequent changes (6:30 PM, 7:10 PM, 12:55 PM):** The content of `worker.py` remains identical across these timestamps. This indicates no functional changes were introduced to the file during these updates, possibly representing saves without modifications or system-level actions.

**2. `d:\Projects\KolossusAI\orchestrator\guardrails.py`**
*   **Timestamp:** 5/18/2026, 6:36:52 PM
*   **Key Changes:** This file defines guardrail functions to ensure safe and compliant LLM responses.
    *   `guardrail_refusal`: Checks if a reply is a generic model refusal, primarily blocking short, refusal-only responses.
    *   `guardrail_llm`: Uses an LLM (gpt-4o-mini by default) to perform a permissive safety check, specifically blocking "CLEARLY dangerous content" (weapons, incitement, financial data) and recording usage.
    *   `guardrail_embedding`: Employs embedding-based cosine similarity to detect dangerous topics (malware, violence, self-harm instructions) in replies, using either a self-hosted VLLM embedding service or LiteLLM as a fallback.
    *   `guardrail_identity_rewrite`: Detects if an LLM is claiming to be Claude/Anthropic AI and attempts to rewrite such responses using another LLM. It includes a quick pattern check to avoid unnecessary LLM calls.

**3. `d:\Projects\KolossusAI\tools\schemas.py`**
*   **Timestamp:** 5/18/2026, 6:51:20 PM, 5/19/2026, 1:59:48 PM, 5/19/2026, 2:05:44 PM
*   **Key Changes:** This file serves as a unified registry for tool definitions, including their OpenAI schema, Claude-specific guide, and metadata.
    *   **Initial and subsequent states:** The content remains consistent across all three timestamps, defining two key tools:
        *   `utility_tool`: Described as MANDATORY for file conversion and text extraction (e.g., PDF to text, docx to PDF, CSV to XLSX, archiving). It emphasizes never using custom Python or shell scripts for these tasks and runs as a sandbox-native tool. Detailed usage examples, coverage matrix, and response contracts are provided.
        *   `knowledge_base_search`: For searching internal knowledge bases using hybrid semantic and keyword search, with a focus on citing sources and retrieving results only from workforce-attached documents.
    *   **Patterns:** The file clearly defines "SYSTEM CONSTRAINTS" for all agents, notably restricting shell_exec to basic file operations and mandating `utility_tool` for all file format/extraction tasks. The structure consistently includes `schema`, `guide`, and `metadata` for each tool. The content for this file also remains identical across multiple timestamps.

**4. `d:\Projects\KolossusAI\orchestrator\multi_agent_router.py`**
*   **Timestamp:** 5/20/2026, 7:13:59 PM
*   **Key Changes:** This module introduces an "Intelligent Agent Coordination with Graph Memory" system.
    *   It outlines an architecture for graph memory integration, intelligent coordination (coordinator agent pattern), execution management, and context propagation.
    *   Defines `AgentRole` (e.g., COORDINATOR, EXECUTOR, RESEARCHER, ANALYST, WRITER, REVIEWER, SPECIALIST, PLANNER) and `MessageType` for inter-agent communication.
    *   Introduces `AgentMessage`, `AgentCapability`, `Subtask`, `SharedMemory`, and `ExecutionPlan` dataclasses to structure multi-agent operations.
    *   The `MultiAgentRouter` class is central, handling agent loading, preferred agent selection, and managing execution plans. It uses visual logging helpers (`_log_banner`, `_log_agent_step`) for clarity.
    *   Includes a check for `litellm` availability.

**5. `d:\Projects\KolossusAI\scripts\create_section_writer_agents.py`**
*   **Timestamp:** 5/20/2026, 11:35:36 AM
*   **Key Changes:** This is a one-off Django shell script designed to create an "EIA Section Writer - Agent A" for filling sections of a Kadam EIA Report template using data from a Project Feasibility Report (PFR).
    *   **Agent A's Instruction:** The agent is given extremely detailed "GROUND RULES" and "ABSOLUTE RULES — NEVER VIOLATE". These rules are highly prescriptive, dictating:
        *   Never modifying heading text or writing section numbers.
        *   Using `[DATA REQUIRED: fieldName]` for missing data.
        *   Creating real Word tables, not plain text.
        *   Leaving static black text and existing tables as is, filling empty cells.
        *   Erasing red instruction text after following.
        *   Writing in clean black text.
        *   Strictly limiting PFR sections to read (Executive Summary, Introduction, Project Description).
        *   Always writing custom Python scripts for file operations (using `python-docx`, `pdfplumber`, `fitz`, `zipfile`) and **never** using `template_parser` or built-in document editing tools.
        *   Never using web fetch/search or spawning sub-agents.
        *   Never deleting existing headings/sections.
        *   Avoiding content repetition.
        *   Skipping the "Terms of Reference" section.
        *   Directly copying text from PFR if section names match, only stripping color.
        *   Using direct XML deep copy for "Summary and Conclusions" from PFR.
        *   Strict rules on sourcing content (text, tables, figures, photographs) for "Introduction" and "Project Description" sections to avoid cross-pollination from other PFR sections and handling duplicates.
        *   Rigorous table/figure/photograph numbering based on chapter number and continuous counters, with a specific logic to initialize counters by scanning the existing document.
        *   Permission to add mini subsections.

**6. `d:\Projects\KolossusAI\workforce\tasks.py`**
*   **Timestamp:** 5/19/2026, 1:19:36 PM, 5/19/2026, 1:19:55 PM, 5/19/2026, 1:33:59 PM
*   **Key Changes:** This file contains Celery tasks for workforce scheduling and generic operations.
    *   **Initial state (1:19 PM):** Defines tasks related to scheduled workforce execution, workspace cleanup, health checks, token flushing, S3 backups, and sending workforce responses.
        *   `on_celery_startup`: Connects to `worker_ready` signal to run subscription checks and session recovery. It also cleans up stale user-scoped approval requests.
        *   `send_schedule_status`, `send_schedule_message`, `send_clear_chat`: Functions to send WebSocket updates to relevant chat rooms for workforce status, messages, and chat clearing.
        *   `_get_report_kb_context`: Retrieves knowledge base context for report refreshes based on the last user message.
        *   `_parse_report_script_blocks`: Parses script content into structured blocks, handling different header types.
        *   `_extract_verification_footer`: Extracts and removes a `__REPORT_OK__` marker from output.
        *   `_extract_html_content`: Extracts HTML content, stripping markers and surrounding commentary, with a fallback for already clean HTML.
        *   `_read_html_from_sandbox`: A fallback function to read HTML files directly from a sandbox.
    *   **Subsequent changes (1:19 PM, 1:33 PM):** The content of `workforce\tasks.py` is identical across these three timestamps, indicating no functional modifications.

**7. `d:\Projects\KolossusAI\governance\enforcement.py`**
*   **Timestamp:** 5/19/2026, 1:37:03 PM
*   **Key Changes:** This file implements a policy enforcement engine with two layers: LLM-based checks and system instruction injection.
    *   **Helper functions:** Includes functions to retrieve default AI models, workforce owner/tenant IDs, and various types of active policy rules (user-scoped, tenant-scoped, connector-based). It also provides logic to resolve who should review an approval request.
    *   `_check_remaining_uses`: Checks if an `ApprovalRequest` has remaining uses, handling permanent approvals (`-1`) and one-time approvals (`>0`).
    *   `check_policies_with_llm`: A core function that uses an LLM to evaluate user messages and context against active keyword policies. It filters out rules that have permanent approvals and logs skipped policies.

**8. `d:\Projects\KolossusAI\scripts\db_agent_system_instruction.txt`**
*   **Timestamp:** 5/21/2026, 7:24:19 PM to 5/21/2026, 7:46:43 PM
*   **Key Changes:** This text file outlines system instructions for a "Database Agent" tasked with extracting data from a Parsec print estimate PDF and inserting it into a SQL Server database. This agent is specifically for "Impact Printers & Lithographers."
    *   **Core Task:** Read PDF, extract job data, insert into SQL Server.
    *   **Database Credentials:** Explicitly lists SQL Server connection details (Server, Database, User, Password).
    *   **Absolute Rules:** Strict rules prohibiting NULL inserts, invalid procedure runs, skipping validation, and requiring all 305 `ProjectServiceValue` rows before a stored procedure call. Also mandates re-validation upon user correction.
    *   **Step-by-step Process:**
        *   **STEP 0 - GET USER EMAIL:** Initially asked the user for email, then modified to *first* check memory for a stored email, and only if not found, ask the user. The DB lookup for `Account.Users` was updated to include `PhoneNumber` and adjusted `UserName` derivation. Robust error handling and re-prompting for email if no match is found were added.
        *   **STEP 1 - LOOK UP REQUIRED KEYS:** Fetches `ClientKey` (initially for 'Impact Printers', later changed to 'Deutsche Bank' with a `NOTE FOR SETUP` to replace), `ServiceKey` (for 'Parsec'), and `CompanyName`.
        *   **STEP 2 - INSERT PEW.Project:** Generates a `NEWID()` for `ProjectKey`, auto-generates `JN-XXXXX` and `EN-XXXXX` job numbers by incrementing the latest existing one, and inserts a new row into `PEW.Project`.
        *   **STEP 3 - INSERT PEW.ProjectService:** Generates `ProjectServiceKey` and inserts into `PEW.ProjectService`.
        *   **STEP 4 - EXTRACT SECTION A VALUES FROM PDF:** Details the extraction of 64 fields from the PDF, categorizing them as "FROM PDF DIRECTLY," "FROM PDF (CUSTOMER INFO — use DB fallback if blank on PDF)," and "DERIVED FROM ColorConfigration."
    *   **Specific Modifications over time:**
        *   **7:24:19 PM:** Initial detailed instructions, `ClientKey` set for '%Impact Printers%'.
        *   **7:30:57 PM:** Minor grammatical change in "To connect to database use credential." and **Crucially, the client company for 'Client Key' (Step 1a) was changed from '%Impact Printers%' to '%Deutsche Bank%', with a setup note.**
        *   **7:31:05 PM - 7:34:46 PM:** Very minor textual corrections around "To connect to database use credentials" (e.g., "tehe Database connection:" -> "the credentials stored u Database connection:" -> "the credentials stored in the secure credent Database connection:" -> "the credentials stored in the secure credentials. Database connection:"). The core logic and client name remain `Deutsche Bank`.
        *   **7:46:43 PM:** The biggest change to `STEP 0`. It was refactored to first check memory for a stored email, only asking the user if not found. The DB lookup now includes `PhoneNumber`, and the `UserName` derivation includes `PhoneNumber`. Enhanced error handling for DB lookup was added.

**9. `Untitled-3`**
*   **Timestamp:** 5/22/2026, 11:18:02 AM
*   **Key Changes:** This file is empty, suggesting it might be a temporary or new file that was saved but not yet populated with code or content.

### Patterns and Recurring Elements:

*   **Django/Python Ecosystem:** The project heavily uses Django (models, transactions, settings) and common Python libraries.
*   **AI/LLM Focus:** Many files (`worker.py`, `guardrails.py`, `multi_agent_router.py`, `db_agent_system_instruction.txt`, `create_section_writer_agents.py`) are centered around AI agent functionality, including LLM interactions, tool usage, memory, planning, and safety.
*   **Structured Instructions/Schemas:** There's a strong emphasis on providing clear, detailed, and often rule-based instructions to LLM agents, either through Python schemas (`tools/schemas.py`) or detailed text files (`scripts/db_agent_system_instruction.txt`, `scripts/create_section_writer_agents.py`). This suggests an attempt to tightly control agent behavior.
*   **Modularity:** The orchestrator module is highly modular, importing many specific sub-modules for distinct functionalities like bundler, context budgeting, emitter, entities, facts, error learning, goals, memory, models, planner, prompt builder, retrieval, Redis utilities, responder, summarizer, tool runner, usage tracker, log utilities, vector store, fact check, and guardrails.
*   **Error Handling and Robustness:** Many functions include `try-except` blocks and logging for error handling, indicating a focus on stability. Functions like `create_tool_turn_safe` explicitly use database locking to prevent race conditions.
*   **Concurrency and Asynchronous Operations:** The use of `async_to_sync` and `channels.layers` in `workforce/tasks.py` points to asynchronous communication for real-time updates (WebSockets).
*   **Database Interactions:** Frequent direct SQL queries (SELECT, INSERT) are specified in agent instructions, particularly in `db_agent_system_instruction.txt`.
*   **File Processing and Conversion:** The `utility_tool` in `tools/schemas.py` and instructions in `create_section_writer_agents.py` highlight a critical need for robust file conversion and extraction capabilities, often prohibiting direct shell/Python for these tasks in favor of a specialized tool.
*   **Agent Identity and Rules:** Specific agents are given explicit identities (e.g., "EIA Section Writer — Agent A", "Database Agent for Impact Printers & Lithographers") and are bound by "ABSOLUTE RULES" or "GROUND RULES" to guide their operations and maintain consistency.
*   **Client Customization:** The `db_agent_system_instruction.txt` specifically includes a "NOTE FOR SETUP" to replace `Deutsche Bank` with the actual client name, indicating this instruction file is a template for client-specific agent deployments.
*   **Iterative Refinement of Agent Instructions:** The `db_agent_system_instruction.txt` shows multiple small, quick changes (7:24 PM to 7:46 PM on 5/21/2026), particularly refining the "GET USER EMAIL" step and correcting typos. This suggests active development and fine-tuning of agent prompts and rules.
*   **Security and Governance:** The presence of `guardrails.py` and `governance/enforcement.py` clearly indicates built-in mechanisms for security, content moderation, and policy compliance, including LLM-based checks, embedding-based checks, and approval workflows.

## 1:54:24 PM
The provided log details changes across multiple Python files within a `d:\Projects\KolossusAI` directory, focusing on orchestrator functionalities, tool schemas, agent system instructions, and multi-agent coordination, spanning from May 18-22, 2026.

**File-Specific Updates:**

*   **`d:\Projects\KolossusAI\orchestrator\worker.py` (May 18, 2026, 6:29:37 PM, 6:30:43 PM, and 7:10:06 PM; May 19, 2026, 12:55:14 PM):**
    *   These timestamps show a stable state of this file over several changes, implying no functional code changes were made across these specific log entries. The content consistently includes extensive imports covering logging, data handling (base64, json, uuid, hashlib), system utilities (os, re, subprocess, sys, time, Path, typing), Django ORM, and various custom orchestrator modules (bundler, context\_budget, emitter, entities, facts, utils, error\_learning, goals, memory, models, planner, prompt\_builder, retrieval, redis\_utils, responder, summarizer, tool\_runner, usage\_tracker, log\_utils, vector\_store, fact\_check, guardrails).
    *   It imports specific workforce modules like `WorkforceLogger`, `_trigger_response_sending`, `Workforce`, `WorkforceMessage`, and `Group`.
    *   There's conditional import for `PIIProcessorFactory` (PII protection) and `MultiAgentRouter`, `MultiAgentExecutor`, `route_task` (multi-agent router), indicating optional features based on import success.
    *   Graph memory context building (`build_graph_memory_for_claude_code`, `build_context_with_retrieval`) and execution planning (`create_execution_plan`) modules are also conditionally imported.
    *   A warning `"[WORKER] Graph memory modules not available"` is logged if graph memory modules aren't found.
    *   It defines a `_instruction_cache` dictionary and conditionally imports `litellm`.
    *   Key functions include `create_tool_turn_safe` (creates `OrchTurn` with row locking), `_sanitize_text` (trims and limits text), `_get_model_pricing` (retrieves AI model pricing from DB or static config), `_clean_file_links_in_response` (removes or replaces file links in LLM responses), and `_normalize_chart_output` (converts chart data to standard markdown format). These functions collectively support core orchestration logic, model interaction, and response formatting.

*   **`d:\Projects\KolossusAI\orchestrator\guardrails.py` (May 18, 2026, 6:36:52 PM):**
    *   This file focuses on various guardrail implementations for LLM responses.
    *   It defines `guardrail_refusal` to detect if an LLM's response is a self-refusal, primarily for short responses.
    *   `guardrail_llm` uses another LLM call to check for dangerous content based on specific prompt instructions, being "VERY PERMISSIVE" and only refusing for clear threats or sensitive data.
    *   `guardrail_embedding` performs an embedding-based refusal check, calculating cosine similarity against known dangerous topics (malware, violence, self-harm instructions) and triggering if similarity is very high (>0.85). It supports both self-hosted VLLM embeddings and LiteLLM/OpenAI as a fallback.
    *   `guardrail_identity_rewrite` checks if an LLM response is claiming an identity (e.g., "I am Claude") and, if so, uses an LLM to rewrite it to remove such claims. This function first performs a quick keyword check before resorting to an LLM call.

*   **`d:\Projects\KolossusAI\tools\schemas.py` (May 18, 2026, 6:51:20 PM; May 19, 2026, 1:59:48 PM; May 19, 2026, 2:05:44 PM):**
    *   These entries indicate no significant changes to the content, showing a consistent definition of tool schemas.
    *   The file acts as a "Unified Tool Schemas for KolossusAI," defining tools like `utility_tool` and `knowledge_base_search`.
    *   `utility_tool` is described as "MANDATORY for any file conversion or text extraction task" with `convert` and `extract_text` actions, covering a wide range of formats (office docs, PDFs, data, images, archives, markdown). It explicitly forbids custom Python or shell scripts for these tasks. It also specifies `execution: "sandbox_native"`, meaning it runs inside a sandbox manager.
    *   `knowledge_base_search` is for searching attached knowledge bases using semantic and keyword hybrid search.
    *   System constraints emphasize using `utility_tool` for file operations and limiting `shell_exec` to basic file ops.

*   **`d:\Projects\KolossusAI\workforce\tasks.py` (May 19, 2026, 1:19:36 PM, 1:19:55 PM, and 1:33:59 PM):**
    *   Similar to `orchestrator\worker.py`, these timestamps show a stable state of this file with no functional code changes across these specific log entries.
    *   The file defines Celery tasks for workforce scheduling and generic operations.
    *   `on_celery_startup` connects to the `worker_ready` signal to perform subscription checks and recovery of orphaned orchestrator sessions. It also includes cleanup for stale user-scoped pending approvals.
    *   Functions for sending WebSocket status (`send_schedule_status`), chat messages (`send_schedule_message`), and clear chat signals (`send_clear_chat`) are present.
    *   Internal helper functions `_get_report_kb_context`, `_parse_report_script_blocks`, `_extract_verification_footer`, `_extract_html_content`, and `_read_html_from_sandbox` are defined, primarily for handling report generation, script block parsing, and content extraction from sandbox output.

*   **`d:\Projects\KolossusAI\governance\enforcement.py` (May 19, 2026, 1:37:03 PM):**
    *   This file outlines the policy enforcement engine for KolossusAI.
    *   It details functions for retrieving policy rules based on scope (user, tenant, connector) and workforce IDs (`_get_workforce_owner_id`, `_get_workforce_tenant_id`, `_get_user_scope_rules`, `_get_tenant_scope_rules`, `_get_connector_rules`, `_get_all_rules`, `_get_workforce_connector_type_ids`).
    *   It resolves approval targets (`_resolve_approval_target`) based on rule scope.
    *   `_check_remaining_uses` verifies if an approved `ApprovalRequest` still has uses, handling permanent approvals (`-1`) and one-time approvals (`>0`).
    *   `check_policies_with_llm` uses an LLM to evaluate user messages and context against active policies, returning a list of violations. It filters out permanently approved rules before the LLM check.

*   **`d:\Projects\KolossusAI\orchestrator\multi_agent_router.py` (May 20, 2026, 7:13:59 PM):**
    *   This file introduces the multi-agent router for intelligent agent coordination with graph memory.
    *   It defines a `MultiAgentRouter` class with `Graph Memory Integration`, `Intelligent Coordination`, `Execution Management`, and `Context Propagation` features.
    *   Includes visual logging helpers `_log_banner` and `_log_agent_step`.
    *   Defines `AgentRole` (e.g., COORDINATOR, EXECUTOR, RESEARCHER) and `MessageType` (e.g., TASK_ASSIGNMENT, HELP_REQUEST) enums.
    *   Data classes `AgentMessage`, `AgentCapability`, `Subtask`, `SharedMemory`, and `ExecutionPlan` structure the multi-agent system.
    *   `SharedMemory` includes `facts`, `entities`, `discoveries`, `agent_notes`, `conversation_context`, `user_preferences`, and `task_history`, with methods to add discoveries and notes, and `get_context_for_agent`.
    *   The `MultiAgentRouter` constructor initializes with `workforce_id`, `session`, and `use_graph_memory`.
    *   `set_preferred_agents` allows specifying agents for direct routing or task decomposition preference.
    *   `_load_agents` loads agent capabilities and group instructions from the workforce.

*   **`d:\Projects\KolossusAI\scripts\create_section_writer_agents.py` (May 20, 2026, 11:35:36 AM):**
    *   This script creates an "EIA Section Writer - Agent A" with specific instructions for generating content for sections (Introduction, Project Description, Summary & Conclusions) of a Kadam EIA Report template using data from a Project Feasibility Report (PFR).
    *   It outlines strict "GROUND RULES" for the agent, including:
        *   Never modifying heading text or writing section numbers.
        *   Filling empty sections with `[DATA REQUIRED: fieldName]`.
        *   Always creating real Word tables, not plain text.
        *   Leaving static black text and existing tables as is, filling empty cells.
        *   Erasing red instruction text after following.
        *   Writing in clean black text.
        *   Strict limitations on data sources (only PFR Executive Summary, Introduction, Project Description for specific sections).
        *   Prohibition of `template_parser` and built-in document editing tools; requiring custom Python scripts using `python-docx`, `pdfplumber`, `fitz`, `zipfile`.
        *   Prohibition of web fetches, web searches, sub-agents, or the Agent tool.
        *   Never deleting existing headings/sections.
        *   Avoiding content repetition.
        *   Not filling the "Terms of Reference" section.
        *   Direct XML deep copy for "Summary and Conclusions."
        *   Strict rules for content, tables, figures, and photographs based on the PFR section being filled, and unique numbering/insertion logic.

*   **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction.txt` (Multiple timestamps on May 21, 2026):**
    *   This text file contains the system instructions for a "Database Agent for Impact Printers & Lithographers."
    *   The agent's primary job is to extract job data from a Parsec print estimate PDF and insert it into a SQL Server database using `pymssql`.
    *   The file specifies database connection details (server, database, user, password), which are explicitly *not* summarized here due to the instruction to omit sensitive information.
    *   **Absolute Rules:** Emphasize never inserting NULLs, strict validation, always inserting all 305 `ProjectServiceValue` rows before calling a stored procedure, and re-validating corrections.
    *   **Step 0 (GET USER EMAIL):** Details a process to first check memory for a stored email, then ask the user if not found. It then performs a DB lookup (`SELECT UserKey, UserName, FirstName, LastName, Email, PhoneNumber FROM Account.Users WHERE Email = '<provided_email>'`) and handles results (1 match, 0 matches, multiple matches, DB error).
    *   **Step 1 (LOOK UP REQUIRED KEYS):** Retrieves `ClientKey` (for "Deutsche Bank," with a note to change for other clients), `ServiceKey` (for "Parsec"), and `CompanyName`.
    *   **Step 2 (INSERT PEW.Project):** Generates a `ProjectKey`, auto-generates `JN Number` and `EN Number` (estimate numbers) by incrementing the last existing ones, extracts a description from the PDF, and inserts a new row into the `PEW.Project` table with various hardcoded and derived values.
    *   **Step 3 (INSERT PEW.ProjectService):** Generates `ProjectServiceKey` and inserts a new row into `PEW.ProjectService`.
    *   **Step 4 (EXTRACT SECTION A VALUES FROM PDF):** This is a critical step, instructing the agent to extract 64 specific fields from the PDF, detailing precise source rules (e.g., "Estimate No. (top right of PDF)" for `ParsecEstimateNumber`).
        *   Fields are categorized: directly from PDF, from PDF with DB fallback for customer info, and derived from `ColorConfigration`.
        *   The derived fields involve parsing "X/Y" color configurations to set `IsInkColor`, `IsInkBlack`, `IsInkBleeds`, `ColorSidOne`, `ColorSidTwo`, `BlackSidOne`, and `BlackSidTwo`.
    *   **Pattern:** Multiple minor updates to the introduction of the system instruction text on May 21, 2026, slightly rephrasing how the database credentials are mentioned. The core instructions and steps remain consistent across these rapid changes.

**Overall Patterns and Recurring Elements:**

*   **Modular Architecture:** The project uses a highly modular structure, with distinct files for different functionalities (orchestration, guardrails, tools, tasks, governance, multi-agent coordination).
*   **Database Interaction:** Recurring patterns of database interaction are evident, particularly in `orchestrator\worker.py` (e.g., `create_tool_turn_safe` with `SELECT FOR UPDATE`, `_get_model_pricing` with `AIModel` lookups) and extensively in `scripts\db_agent_system_instruction.txt` for SQL Server insertions and lookups.
*   **LLM Integration and Control:** The system heavily relies on LLMs, with specific modules for prompt building, response extraction, and various guardrails (`guardrail_refusal`, `guardrail_llm`, `guardrail_embedding`, `guardrail_identity_rewrite`). The multi-agent router further indicates sophisticated LLM orchestration.
*   **Context Management and Memory:** Concepts of context (e.g., `budget_context`, `SharedMemory`), entities, facts, and memory nodes are central to the orchestrator and multi-agent systems, suggesting a focus on maintaining state and knowledge across interactions.
*   **File Handling and Conversion:** The `utility_tool` in `tools\schemas.py` and file-related functions in `orchestrator\worker.py` (e.g., `_clean_file_links_in_response`) highlight a strong capability for processing, converting, and extracting information from various file types.
*   **System Instructions/Agents:** Detailed, step-by-step instructions for agents (e.g., "EIA Section Writer - Agent A," "Database Agent for Impact Printers & Lithographers") are defined as system prompts, demonstrating a prompt-driven agent behavior configuration. These instructions often include "ABSOLUTE RULES" and explicit step breakdowns.
*   **Asynchronous Processing:** The use of Celery (`workforce\tasks.py`) and asynchronous utilities (`asgiref.sync.async_to_sync`) points to a distributed and event-driven processing model, especially for long-running or scheduled tasks.
*   **Conditional Features:** Many advanced features (PII protection, multi-agent routing, graph memory, LiteLLM) are loaded conditionally using `try-except` blocks, indicating flexibility in deployment and environment configurations.
*   **Logging and Monitoring:** Extensive logging is present throughout, often with context-specific prefixes (e.g., `[WORKER]`, `[GUARDRAIL]`, `[ROUTER]`, `[SCHEDULER]`, `[REPORT]`) to track operations and potential issues.
*   **Security and Governance:** Modules like `guardrails.py` and `governance\enforcement.py` indicate a focus on security, safety, and policy adherence in AI agent interactions, including LLM-based checks, embedding-based content filtering, and approval workflows.
*   **Time-based Stability:** The repeated identical content for several files across different timestamps (e.g., `orchestrator\worker.py`, `workforce\tasks.py`, `tools\schemas.py`) suggests either automatic saving without actual changes or re-generation/re-formatting that resulted in identical content blocks in the log. Only `scripts\db_agent_system_instruction.txt` showed small, iterative changes to the phrasing of its initial database connection sentence within a short time frame.

## 2:54:17 PM
The provided logs indicate recent modifications across several Python modules within a "KolossusAI" project, focusing on core orchestrator logic, tool definitions, agent instructions, and multi-agent coordination, along with minor updates to environment variables.

### File-Specific Updates:

1.  **`d:\Projects\KolossusAI\orchestrator\worker.py`**
    *   **Timestamp Range:** 5/18/2026, 6:29:37 PM - 5/18/2026, 6:30:43 PM; 5/18/2026, 7:10:06 PM - 5/19/2026, 12:55:14 PM
    *   **Key Changes:** This file, a central orchestrator worker, saw minor, functionally identical updates across multiple timestamps. The code remained consistent, indicating potential re-saves or minor non-substantive changes (e.g., whitespace, line endings) that didn't alter the core logic.
    *   **Content Summary:** The worker imports a wide array of modules for various AI orchestration tasks: logging, data handling (base64, json, hashlib), concurrency (threading, contextvars), file operations (pathlib, os, subprocess, re), database interactions (Django ORM for `OrchSession`, `OrchTurn`, `AIModel`), and specialized AI functionalities (agent models, context budgeting, reply emission, entity management, fact extraction/reconciliation, error learning, goal management, memory, planning, prompt building, retrieval, Redis utilities for task management, response generation, summarization, tool execution, usage tracking, logging, vector storage, fact/policy checking, and guardrails). It also conditionally imports PII protection and multi-agent routing modules. Key functions defined include `create_tool_turn_safe` (for atomic `OrchTurn` creation), `_sanitize_text` (for text trimming/cleaning), `_get_model_pricing` (for retrieving AI model costs), and `_clean_file_links_in_response` (for normalizing markdown file links in LLM responses). A `_normalize_chart_output` function is present, indicating handling of chart data from tool outputs.

2.  **`d:\Projects\KolossusAI\orchestrator\guardrails.py`**
    *   **Timestamp:** 5/18/2026, 6:36:52 PM
    *   **Key Changes:** This file defines guardrail functions to ensure AI model safety and compliance.
    *   **Content Summary:** It includes `guardrail_refusal` (detects if an LLM's response is a self-refusal, primarily if it's short and contains refusal patterns), `guardrail_llm` (uses an LLM to check for dangerous content based on a permissive safety prompt, recording usage), `guardrail_embedding` (uses cosine similarity with embeddings to detect very high similarity to known dangerous topics like malware instructions or self-harm, with fallback to LiteLLM if self-hosted embedding is unavailable), and `guardrail_identity_rewrite` (checks for and rewrites responses where the LLM claims to be a specific AI, e.g., "I am Claude," to maintain consistent persona). The LLM-based checks rely on `litellm_completion_with_fallback` and `record_llm_call` for usage tracking.

3.  **`d:\Projects\KolossusAI\tools\schemas.py`**
    *   **Timestamp Range:** 5/18/2026, 6:51:20 PM - 5/19/2026, 2:05:44 PM
    *   **Key Changes:** This file, which centralizes tool definitions, was repeatedly saved without apparent functional changes.
    *   **Content Summary:** It defines a `TOOLS` registry containing schemas (OpenAI format), guides (Claude instructions), and metadata for various tools. A prominent "utility_tool" is detailed, designed for file conversion and text extraction across a wide range of formats (office documents, PDFs, data formats, images, archives, markdown). It emphasizes mandatory use of `utility_tool` for file conversions and text extraction, restricting `shell_exec` to basic file operations. It specifies that `utility_tool` is "sandbox-native" and should be called via a `SANDBOX_MANAGER_URL` and `SANDBOX_SESSION_ID`, not directly via the Kolossus API. A `knowledge_base_search` tool is also defined for retrieving information from workforce-attached documents.

4.  **`d:\Projects\KolossusAI\orchestrator\multi_agent_router.py`**
    *   **Timestamp:** 5/20/2026, 7:13:59 PM
    *   **Key Changes:** This file introduces a comprehensive multi-agent routing and coordination system.
    *   **Content Summary:** It outlines an architecture for intelligent agent coordination with graph memory, including integration with shared context (facts, entities, summaries), dynamic task delegation, agent-to-agent communication, and execution management. It defines `AgentRole` (e.g., COORDINATOR, EXECUTOR, RESEARCHER), `MessageType` (e.g., TASK_ASSIGNMENT, HELP_REQUEST), `AgentMessage` (for inter-agent communication), `AgentCapability` (describing an agent's skills), `Subtask` (representing units of work), `SharedMemory` (for agents to share context), and `ExecutionPlan` (for managing multi-agent workflows). The `MultiAgentRouter` class handles agent loading, setting preferred agents for routing, and coordinating tasks.

5.  **`d:\Projects\KolossusAI\scripts\create_section_writer_agents.py`**
    *   **Timestamp:** 5/20/2026, 11:35:36 AM
    *   **Key Changes:** This script defines and creates a specific type of AI agent.
    *   **Content Summary:** It's a one-off script to create an "EIA Section Writer - Agent A" agent. This agent is instructed to fill specific sections (Introduction, Project Description, Summary & Conclusions) of a Kadam EIA Report template using data from a Project Feasibility Report (PFR). The instructions include strict ground rules: never modify headings, avoid section numbers in text, fill empty sections with `[DATA REQUIRED]`, use real Word tables, preserve static text, erase red instruction text, use clean black text, limit PFR reading to specific sections, never use `template_parser` or built-in document editing tools (instead, write Python scripts with `python-docx`, `pdfplumber`, etc.), no web fetches/searches, no sub-agents, never delete existing headings/sections, avoid content repetition, skip "Terms of Reference," copy exact text for identically named sections, use XML deep copy for "Summary and Conclusions," and enforce strict rules for content and image/table insertion based on the current PFR section and dynamic numbering.

6.  **`d:\Projects\KolossusAI\workforce\tasks.py`**
    *   **Timestamp Range:** 5/19/2026, 1:19:36 PM - 5/19/2026, 1:33:59 PM
    *   **Key Changes:** This file, containing Celery tasks, also experienced multiple saves with identical code.
    *   **Content Summary:** It defines background tasks for workforce scheduling and operations. On Celery worker startup, it connects to run subscription checks and session recovery, and cleans up stale pending approval requests. It includes functions to send real-time status updates (`send_schedule_status`), chat messages (`send_schedule_message`), and clear chat signals (`send_clear_chat`) via WebSockets to workforce rooms. Helper functions `_get_report_kb_context` (retrieves knowledge base context for reports), `_parse_report_script_blocks` (parses script content into structured blocks), `_extract_verification_footer` (removes a `__REPORT_OK__` marker), `_extract_html_content` (cleans and extracts HTML from output), and `_read_html_from_sandbox` (fallback to read HTML directly from sandbox) are present, primarily for report generation and processing.

7.  **`d:\Projects\KolossusAI\governance\enforcement.py`**
    *   **Timestamp:** 5/19/2026, 1:37:03 PM
    *   **Key Changes:** This file focuses on policy enforcement.
    *   **Content Summary:** It outlines a two-layer policy enforcement engine: LLM-based checks and system instruction injection. It provides helper functions (`_get_default_ai_model`, `_get_default_llm_model`, `_get_workforce_owner_id`, `_get_workforce_tenant_id`, `_get_user_scope_rules`, `_get_tenant_scope_rules`, `_get_connector_rules`, `_get_all_rules`, `_get_workforce_connector_type_ids`, `_resolve_approval_target`) to fetch policy rules and determine approval targets. The `_check_remaining_uses` function manages one-time and permanent approvals for policy rules. The core `check_policies_with_llm` function uses an LLM to evaluate user messages against active keyword-matching policies, returning a list of violations. It intelligently filters out rules with permanent approvals to avoid redundant checks.

8.  **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction.txt`**
    *   **Timestamp Range:** 5/21/2026, 7:24:19 PM - 5/21/2026, 7:46:43 PM
    *   **Key Changes:** This text file, detailing instructions for a database agent, underwent several rapid modifications, primarily in its introductory paragraphs and "STEP 0" section.
    *   **Content Summary:** The instructions define a "Database Agent for Impact Printers & Lithographers" responsible for extracting data from Parsec PDF estimates and inserting it into a SQL Server database. The agent is given strict rules ("ABSOLUTE RULES — NEVER VIOLATE"), including not inserting NULLs, performing validation, and inserting 305 `ProjectServiceValue` rows before a stored procedure call. "STEP 0" was significantly refined, changing from directly asking the user for email to first checking memory for a stored email, then asking the user if not found, and performing a more robust DB lookup for user accounts, including error handling and multiple match scenarios. Database connection details (Server, Database, User, Password) are explicitly provided. "STEP 1" defines how to look up required keys (`ClientKey`, `ServiceKey`, `CompanyName`), initially mentioning "Impact Printers" as the client but later changing it to "Deutsche Bank" with a "NOTE FOR SETUP" to generalize the client name. "STEP 2" details inserting data into `PEW.Project`, including auto-generating job numbers (`JN-XXXXX`, `EN-XXXXX`). "STEP 3" covers inserting into `PEW.ProjectService`. "STEP 4" specifies the extraction of 64 fields from the PDF, categorizing them into "FROM PDF DIRECTLY," "FROM PDF (CUSTOMER INFO — use DB fallback if blank on PDF)," and "DERIVED FROM ColorConfigration," with precise rules for parsing and deriving values.

### Patterns and Recurring Elements:

*   **Django Framework:** The project heavily uses Django, indicated by imports like `django.conf.settings`, `django.db.transaction`, `django.db.models`, `django.utils.timezone`, and `django.contrib.auth`.
*   **AI/LLM-centric development:** Many modules are geared towards AI capabilities, including orchestrating LLM interactions, managing memory, goals, plans, tool use, and applying guardrails. `litellm` is a recurring import for LLM interaction.
*   **Database Interaction:** SQL Server commands (e.g., `SELECT`, `INSERT`, `NEWID()`, `GETDATE()`) and database models (`OrchSession`, `OrchTurn`, `AIModel`, `Workforce`, `ApprovalRequest`) are prevalent, indicating a robust data persistence and management layer.
*   **Modularity and Conditional Imports:** Modules like `PIIProcessorFactory` and `MultiAgentRouter` are conditionally imported using `try-except ImportError` blocks, suggesting a flexible architecture where certain features can be enabled or disabled based on environment or configuration.
*   **Logging:** Extensive use of `logging` (e.g., `logger.info`, `logger.warning`, `logger.error`) throughout the codebase for tracking operations, debugging, and status reporting. Custom loggers like `OrchestratorLogger` and `WorkforceLogger` are present.
*   **Task Queuing/Asynchronous Processing:** `celery`, `shared_task`, `async_to_sync`, and `channels.layers` indicate the use of asynchronous task processing and WebSockets for real-time communication, particularly in `workforce.tasks.py`.
*   **System Instructions and Rules:** Detailed "system instructions" are a key pattern, especially in `scripts/create_section_writer_agents.py` and `scripts/db_agent_system_instruction.txt`, defining explicit "ABSOLUTE RULES — NEVER VIOLATE" and step-by-step procedures for AI agents. This highlights a strong emphasis on controlled and predictable agent behavior.
*   **File Handling and Conversion:** The `utility_tool` schema and related discussions emphasize a standardized approach to file conversion and text extraction, discouraging custom Python or shell scripts for these tasks.
*   **Security and Governance:** `guardrails.py` and `governance.enforcement.py` highlight a focus on safety, compliance, and controlled access through policies, approvals, and content filtering.
*   **Repetitive Saves/Identical Code:** The `orchestrator\worker.py`, `tools\schemas.py`, and `workforce\tasks.py` files show identical content across multiple, close timestamps. This could imply automated formatting, version control operations, or minor, non-functional edits (e.g., saving without changes).
*   **Relative Paths:** All file paths are Windows-style absolute paths (e.g., `d:\Projects\KolossusAI\`).
*   **Future/Hypothetical Dates:** All timestamps are from 2026, indicating that this log represents planned or future development rather than past changes.

**Note:** The `.env` file, which typically contains sensitive information like API keys and database credentials, was explicitly excluded from summarization as per instructions.

## 3:54:23 PM
The provided log details changes across several Python files and one text file, primarily focusing on updates related to an AI orchestration system, tool definitions, governance, and agent instructions.

**File-Specific Updates:**

*   **`d:\Projects\KolossusAI\orchestrator\worker.py` (Multiple timestamps: 5/18/2026, 6:29:37 PM; 5/18/2026, 6:30:43 PM; 5/18/2026, 7:10:06 PM; 5/19/2026, 12:55:14 PM)**:
    *   These entries show identical code content across four different timestamps. This indicates either no functional changes occurred during these saves/commits, or the changes were reverted, or they were purely whitespace/formatting changes not captured in the diff provided.
    *   The file itself is a core component of an AI orchestrator, responsible for managing AI sessions, turns, plans, and interactions. It imports a wide array of modules for tasks such as logging, context management, database transactions, entity extraction, goal management, memory, prompt building, retrieval, Redis utilities, LLM response handling, summarization, tool execution, usage tracking, and guardrails.
    *   It includes logic for PII protection and multi-agent routing, which are dynamically imported if available.
    *   Key functions defined are `create_tool_turn_safe` (for atomic turn creation), `_sanitize_text` (for text cleaning), `_get_model_pricing` (for AI model cost lookup), and `_clean_file_links_in_response` (to manage file links in LLM output).
    *   A `_normalize_chart_output` function is also present, indicating functionality for standardizing chart data formats.
    *   The consistent content across multiple timestamps suggests a period of stability or minor, non-functional adjustments.

*   **`d:\Projects\KolossusAI\orchestrator\guardrails.py` (Timestamp: 5/18/2026, 6:36:52 PM)**:
    *   This file defines various guardrail functions to ensure safe and compliant LLM interactions.
    *   `guardrail_refusal`: Checks if an LLM's reply is a generic refusal, especially for short responses.
    *   `guardrail_llm`: Uses another LLM to perform a more sophisticated safety check on the response, being "VERY PERMISSIVE" and only refusing for "CLEARLY dangerous content."
    *   `guardrail_embedding`: Implements an embedding-based refusal check using cosine similarity against a list of blocked topics (malware, violence, self-harm). It can use self-hosted VLLM or fallback to `text-embedding-3-small`.
    *   `guardrail_identity_rewrite`: Detects and rewrites LLM responses that claim to be a specific AI (e.g., "I am Claude/Anthropic AI"). It first checks for identity patterns and then uses an LLM for rewriting if patterns are found.
    *   The presence of `LITELLM_AVAILABLE` flags indicates conditional imports and functionality depending on the `litellm` library.

*   **`d:\Projects\KolossusAI\tools\schemas.py` (Multiple timestamps: 5/18/2026, 6:51:20 PM; 5/19/2026, 1:59:48 PM; 5/19/2026, 2:05:44 PM)**:
    *   This file acts as a "Unified Tool Schemas" registry for KolossusAI, defining the `schema`, `guide`, and `metadata` for various tools in an OpenAI format.
    *   It specifies "SYSTEM CONSTRAINTS" for all agents, mandating the use of `utility_tool` for file conversion/extraction and limiting `shell_exec` to basic file operations.
    *   The `utility_tool` schema and guide are extensively detailed, covering a wide range of file format conversions (office docs, PDFs, data formats, images, archives, markdown) and text extraction. It emphasizes that this tool runs as "sandbox-native" and should be called via `SANDBOX_MANAGER_URL`, not the Django API endpoint.
    *   A `knowledge_base_search` tool is also defined, focusing on semantic and keyword hybrid search within attached documents, with a clear instruction to cite sources.
    *   Similar to `orchestrator\worker.py`, these entries for `tools\schemas.py` show identical content across multiple timestamps, suggesting no functional changes were committed in the provided logs for this file.

*   **`d:\Projects\KolossusAI\workforce\tasks.py` (Multiple timestamps: 5/19/2026, 1:19:36 PM; 5/19/2026, 1:19:55 PM; 5/19/2026, 1:33:59 PM)**:
    *   This file defines Celery tasks for workforce scheduling and generic operations within the KolossusAI system.
    *   Tasks include `execute_scheduled_workforce`, `cleanup_expired_workspaces`, `health_check`, `flush_expired_tokens`, `backup_workspace_to_s3_task`, and `send_workforce_response_task`.
    *   It contains `@worker_ready.connect` handler that runs `ensure_default_tenant_subscriptions()` and `run_recovery()` for orphaned sessions and stale user-scoped approvals on Celery worker startup.
    *   WebSocket communication functions (`send_schedule_status`, `send_schedule_message`, `send_clear_chat`) are defined to push updates to connected users.
    *   Helper functions like `_get_report_kb_context` (for knowledge base retrieval), `_parse_report_script_blocks` (for parsing agent scripts), `_extract_verification_footer` (for removing report markers), and `_extract_html_content` (for parsing HTML from output) are also present.
    *   The content is identical across the three timestamps, again suggesting no functional changes were made or recorded in these specific log entries.

*   **`d:\Projects\KolossusAI\governance\enforcement.py` (Timestamp: 5/19/2026, 1:37:03 PM)**:
    *   This module implements a "Policy Enforcement Engine" with two layers: LLM-based checks and system instruction injection.
    *   It provides helper functions for fetching default AI models, workforce owner/tenant IDs, and various types of policy rules (`_get_user_scope_rules`, `_get_tenant_scope_rules`, `_get_connector_rules`, `_get_all_rules`).
    *   `_resolve_approval_target` determines who should review an approval request based on rule scope.
    *   `_check_remaining_uses` handles approved requests with limited uses, reducing `remaining_uses` for tenant-scoped approvals.
    *   `check_policies_with_llm` is the core function that uses an LLM to evaluate user messages and context against active keyword policies, returning a list of violations. It filters out rules that have permanent approval.

*   **`d:\Projects\KolossusAI\orchestrator\multi_agent_router.py` (Timestamp: 5/20/2026, 7:13:59 PM)**:
    *   This file introduces the "Multi-Agent Router" for intelligent agent coordination using graph memory.
    *   It defines an architecture with graph memory integration, intelligent coordination (coordinator agent pattern, dynamic task delegation), execution management (parallel/sequential, dependency-aware), and context propagation.
    *   Key dataclasses are `AgentMessage` (for inter-agent communication), `AgentCapability` (describing an agent's roles, tools, load), `Subtask` (representing a step in a workflow), and `SharedMemory` (for facts, entities, discoveries, notes, conversation context).
    *   The `MultiAgentRouter` class manages agents, active plans, message queues, and implements agent loading with capability analysis from workforce groups. It also allows setting "preferred agents" for targeted routing.
    *   Visual logging helpers (`_log_banner`, `_log_agent_step`) are included for better debug visibility.

*   **`d:\Projects\KolossusAI\scripts\create_section_writer_agents.py` (Timestamp: 5/20/2026, 11:35:36 AM)**:
    *   A one-off script for creating "EIA Section Writer - Agent A" with specific instructions for filling sections of an EIA Report template using data from a Project Feasibility Report (PFR).
    *   The agent's "GROUND RULES" are highly detailed, emphasizing:
        *   Never modifying heading text or section numbers.
        *   Filling empty sections with `[DATA REQUIRED: fieldName]`.
        *   Using real Word tables, not plain text.
        *   Retaining static black text and existing tables.
        *   Erasing red instruction text after following.
        *   Writing in clean black text.
        *   Strictly limiting data extraction to specific PFR sections (Executive Summary, Introduction, Project Description).
        *   Using custom Python scripts (`python-docx`, `pdfplumber`, etc.) for file operations, *never* `template_parser` or built-in tools.
        *   Prohibiting web fetching, web search, sub-agents, or deleting existing headings.
        *   Avoiding content repetition.
        *   Specific rules for copying text verbatim if section names match.
        *   Mandating direct XML deep copy for "Summary and Conclusions."
        *   Strict rules for image and table insertion, including section-specific sourcing and sequential numbering based on chapter.

*   **`d:\Projects\KolossusAI\scripts\db_agent_system_instruction.txt` (Multiple timestamps: 5/21/2026, 7:24:19 PM; 5/21/2026, 7:24:26 PM; 5/21/2026, 7:30:57 PM; 5/21/2026, 7:31:05 PM; 5/21/2026, 7:31:14 PM; 5/21/2026, 7:31:23 PM; 5/21/2026, 7:33:38 PM; 5/21/2026, 7:33:47 PM; 5/21/2026, 7:34:05 PM; 5/21/2026, 7:34:35 PM; 5/21/2026, 7:34:41 PM; 5/21/2026, 7:34:46 PM)**:
    *   This is a system instruction text for a "Database Agent" whose job is to read Parsec print estimate PDFs, extract job data, and insert it into a SQL Server database.
    *   **Recurring elements/patterns**:
        *   **Database Credentials**: Explicitly states `Server`, `Database`, `User`, `Password` for SQL Server connection using `pymssql`. These credentials are provided directly in the instruction.
        *   **Absolute Rules**: A set of strict rules are consistently maintained across all versions, including:
            *   Never insert NULL (use `''` or `'0'`).
            *   Never run stored procedures on invalid/empty Section A values.
            *   Always validate fields.
            *   Insert all 305 `ProjectServiceValue` rows before calling stored procedure.
            *   Re-validate after user correction.
        *   **Multi-Step Process**: The instruction is broken down into clear steps:
            *   **STEP 0 — GET USER EMAIL**: Asks for user email, performs a `SELECT` query on `Account.Users`, handles 0, 1, or multiple matches, and saves user details. A change from "ASK USER FOR EMAIL" to "GET USER EMAIL" and added logic to check memory for stored email first. The DB column for phone changed from `Phone` to `PhoneNumber` between 7:24:26 PM and 7:46:43 PM.
            *   **STEP 1 — LOOK UP REQUIRED KEYS**: Queries for `ClientKey` (for "Deutsche Bank"), `ServiceKey` (for "Parsec"), and `CompanyName`. A `NOTE FOR SETUP` is added to explicitly mention replacing "Deutsche Bank" for different clients.
            *   **STEP 2 — INSERT PEW.Project**: Generates a `NEWID()` for `ProjectKey`, auto-generates `JN` and `EN` job numbers based on previous entries, extracts `Description` from PDF, and `INSERT`s into `PEW.Project`. The `ProjectTypeKey` is a fixed GUID.
            *   **STEP 3 — INSERT PEW.ProjectService**: Generates a `NEWID()` for `ProjectServiceKey` and `INSERT`s into `PEW.ProjectService`.
            *   **STEP 4 — EXTRACT SECTION A VALUES FROM PDF**: A detailed list of 64 fields to extract from the PDF, specifying whether they are directly from PDF, derived, or use DB fallback. This section is extensive and consistent across all logged changes.
    *   **Evolution of "Database connection" instruction**:
        *   Initial: "To connect to database use credential." (7:24:19 PM)
        *   Slight grammar fix: "To connect to database use credential ." (7:24:26 PM)
        *   Added detail: "To connect t Database connection:" (7:30:57 PM)
        *   More specific: "To connect to database use tehe Database connection:" (7:31:05 PM)
        *   Refined: "To connect to database use the credentials stored u Database connection:" (7:31:14 PM)
        *   Finalized: "To connect to database use the credentials stored in the secure credentials." (7:31:23 PM onwards), followed by the specific database connection details. The database credentials themselves are explicitly written out in plain text as part of the system instruction.
    *   Minor edits in `db_agent_system_instruction.txt` between timestamps are primarily grammatical fixes or slight rephrasing of the introduction and the "STEP 0" section. For example, the instruction to get user email became more robust, first checking memory before prompting the user, and updated the field name for phone number from `Phone` to `PhoneNumber`.

**Patterns and Recurring Elements:**

*   **Django Application Structure**: The file paths (`d:\Projects\KolossusAI\orchestrator`, `d:\Projects\KolossusAI\workforce`, `d:\Projects\KolossusAI\tools`, `d:\Projects\KolossusAI\governance`, `d:\Projects\KolossusAI\scripts`) indicate a well-organized Django project, with distinct modules for core orchestration, workforce management, tool definitions, governance, and utility scripts.
*   **AI/LLM Focus**: A strong theme across multiple files is the integration and management of AI models and Large Language Models (LLMs).
    *   `orchestrator/worker.py` heavily imports LLM-related modules (`responder`, `summarizer`, `usage_tracker`, `guardrails`).
    *   `orchestrator/guardrails.py` explicitly implements various LLM-based safety checks.
    *   `orchestrator/multi_agent_router.py` describes intelligent task routing for multiple agents, with an underlying assumption of LLM-driven decision-making.
    *   `tools/schemas.py` defines tools for LLM agents to use.
    *   `scripts/create_section_writer_agents.py` and `scripts/db_agent_system_instruction.txt` define detailed system instructions for specialized LLM agents.
*   **Database Interactions**: Persistent storage and retrieval from a database (specifically PostgreSQL based on `IS_SQLITE3=False` in `.env`, and SQL Server mentioned in agent instructions) are central.
    *   `orchestrator/worker.py` uses `django.db.transaction` and `OrchSession.objects.select_for_update()`.
    *   `governance/enforcement.py` interacts with `PolicyRule` and `ApprovalRequest` models.
    *   `scripts/db_agent_system_instruction.txt` is entirely focused on SQL queries for data insertion and lookup.
*   **Modular Design**: The codebase demonstrates a modular design, with separate files for different functionalities (e.g., `bundler`, `context_budget`, `emitter`, `entities`, `facts`, `goals`, `memory`, `planner`, `prompt_builder`, `retrieval`, `redis_utils`, `responder`, `summarizer`, `tool_runner`, `usage_tracker`, `log_utils`, `vector_store`, `fact_check`, `guardrails`, `multi_agent_router`).
*   **Error Handling and Logging**: `try-except` blocks are prevalent for robust error handling, and `logging` is extensively used for tracking operations and debugging.
*   **Timestamp Consistency**: For several files (`worker.py`, `tools/schemas.py`, `workforce/tasks.py`), multiple log entries show *identical* code content at different timestamps on the same day. This suggests either frequent saves without functional changes, or the provided log format duplicates entries without showing actual diffs if only minor non-functional changes occurred (e.g., whitespace).
*   **Agent-Specific Instructions**: The `scripts` directory contains detailed system instructions for specialized AI agents (EIA Section Writer, Database Agent), highlighting a pattern of highly customized agent behavior and task definitions. These instructions are verbose and cover numerous rules, constraints, and step-by-step procedures.
*   **Policy and Governance**: The `governance/enforcement.py` file indicates a significant focus on policy enforcement, approvals, and safety checks for agent operations, including user-scoped, tenant-scoped, and connector-based rules.
*   **Tool Usage Emphasis**: `tools/schemas.py` explicitly outlines how agents should use tools, mandating specific tools for certain tasks (like `utility_tool` for file operations) and restricting others (like `shell_exec`). This suggests a controlled environment for agent actions.
*   **Pydantic Warnings Suppression**: `warnings.filterwarnings("ignore", message=".*Pydantic.*", ...)` in `worker.py` is a recurring pattern, indicating an ongoing issue or decision to suppress Pydantic-related warnings during development or runtime.