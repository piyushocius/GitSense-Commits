# Activity Summary for 5/19/2026

## 12:54:03 PM
The `d:\Projects\KolossusAI\orchestrator\worker.py` file, observed at 5/18/2026, 6:29:37 PM and 6:30:43 PM (with identical content), functions as the core orchestrator component. It imports numerous modules covering a broad range of AI agent functionalities, including context management, goal planning, memory operations, tool execution, LLM interaction, and multi-agent routing. Key functions defined include `create_tool_turn_safe` for atomic turn creation, `_sanitize_text` for content formatting, `_get_model_pricing` for fetching model costs, `_clean_file_links_in_response` for processing markdown links in LLM replies, and `_normalize_chart_output` for standardizing chart data. The file uses `try-except ImportError` blocks to dynamically enable features like PII protection, multi-agent routing, and graph memory, and checks for `LITELLM_AVAILABLE` for conditional `litellm` usage.

The `d:\Projects\KolossusAI\orchestrator\guardrails.py` file, updated on 5/18/2026, 6:36:52 PM, implements several safety guardrails:
*   `guardrail_refusal`: Identifies and blocks LLM responses that are solely a refusal to engage, particularly for short answers.
*   `guardrail_llm`: Leverages a separate, permissive LLM (e.g., `gpt-4o-mini`) to detect and refuse "CLEARLY dangerous content" (e.g., instructions for weapons, incitement to violence, financial data). It records LLM usage for this check.
*   `guardrail_embedding`: Uses embedding similarity to block responses highly similar to known dangerous topics like malware creation or self-harm instructions. It supports both a self-hosted VLLM endpoint and `litellm.embedding` as a fallback, also tracking usage.
*   `guardrail_identity_rewrite`: Detects and rewrites responses where the LLM claims a specific identity (e.g., "I am Claude") by using another LLM call to correct the statement. This is only triggered if specific first-person identity patterns are found.

The `d:\Projects\KolossusAI\tools\schemas.py` file, modified on 5/18/2026, 6:51:20 PM, serves as a centralized registry for AI tool definitions. It mandates the use of `utility_tool` for file conversion and text extraction, restricting `shell_exec` to basic file operations.
*   The `utility_tool` is extensively defined, covering approximately 140 conversion pairs across various document, data, image, and archive formats. It specifies a `convert` action (file-to-file) and an `extract_text` action (file-to-text), and provides detailed usage instructions for sandbox-native execution via `curl` to a `SANDBOX_MANAGER_URL`.
*   The `knowledge_base_search` tool is defined for semantic and keyword hybrid searches within workforce-attached documents, emphasizing source citation.
Each tool entry includes its OpenAI-formatted schema, a comprehensive guide with Claude instructions, and metadata.

**Patterns and Recurring Elements:**
*   **Modular Features:** The use of `try-except ImportError` blocks in `worker.py` for PII protection, multi-agent routing, and graph memory indicates that these functionalities are optionally loaded, possibly controlled by environment variables or feature flags.
*   **LLM Availability Checks:** The `LITELLM_AVAILABLE` boolean is frequently checked before attempting to use `litellm` functionality, showing a pattern of defensive programming against external dependency issues.
*   **Comprehensive Guardrails:** A strong emphasis on AI safety and content moderation is evident through the implementation of multiple guardrail mechanisms (refusal, LLM-based, embedding-based, identity rewriting) in `guardrails.py`.
*   **Usage Tracking:** The `record_llm_call` function is consistently used in `guardrails.py` to log input/output tokens and duration for LLM and embedding calls, highlighting a focus on monitoring and cost attribution.
*   **Standardized Tooling:** The `tools/schemas.py` file establishes a clear pattern for defining tools with a unified structure, including schema, detailed guides, and metadata, promoting consistency and maintainability.
*   **Sandbox Integration:** The explicit instructions for calling `utility_tool` as `sandbox-native` indicate a clear architectural design where certain tools are executed within an isolated sandbox environment, separate from the main Django application.

## 1:54:08 PM
The log captures changes across three Python files within the `KolossusAI` project (`orchestrator/worker.py`, `orchestrator/guardrails.py`, `tools/schemas.py`) and modifications to the `.env` configuration file. The timestamps indicate a period of active development and configuration, primarily on 5/18/2026 and 5/19/2026.

### `d:\Projects\KolossusAI\orchestrator\worker.py` (5/18/2026, 6:29:37 PM, and 5/18/2026, 6:30:43 PM, and 5/19/2026, 12:55:14 PM)

This file, a core component of the AI orchestrator, remained largely stable across the recorded timestamps.
*   **Key Functionality:** It handles session management (`create_tool_turn_safe` with row locking), text sanitization (`_sanitize_text`), model pricing lookup (`_get_model_pricing`), and post-processing of LLM responses, specifically cleaning markdown file links (`_clean_file_links_in_response`) and normalizing chart outputs (`_normalize_chart_output`).
*   **Imports:** A wide array of modules are imported, indicating its central role and reliance on various services: `logging`, `base64`, `os`, `re`, `subprocess`, `sys`, `time`, `uuid`, `hashlib`, `json`, `threading`, `warnings`, `pathlib`, `typing`. Django components (`settings`, `transaction`, `IntegrityError`, `F`, `Max`, `timezone`) are heavily used for database interactions. Custom modules from `.bundler`, `.context_budget`, `.emitter`, `.entities`, `.facts`, `.utils`, `.error_learning`, `.goals`, `.memory`, `.models`, `.planner`, `.prompt_builder`, `.retrieval`, `.redis_utils`, `.responder`, `.summarizer`, `.tool_runner`, `.usage_tracker`, `.log_utils`, `.vector_store`, `.fact_check`, `.guardrails` demonstrate a complex, modular architecture for AI orchestration, including concepts like context budgeting, fact management, goal setting, memory, planning, prompt building, retrieval, Redis utilities for task management, response generation, summarization, tool execution, usage tracking, logging, vector storage, fact/policy checking, and guardrails.
*   **External Integrations:** Includes imports for `channels.layers` (for real-time communication), `asgiref.sync` (for async/sync bridging), and `workforce` models/services.
*   **Conditional Features:** The code conditionally imports and enables features like PII protection (`PIIProcessorFactory`), multi-agent routing (`MultiAgentRouter`, `MultiAgentExecutor`, `route_task`), and graph memory (`build_graph_memory_for_claude_code`, `create_execution_plan`), indicating a flexible architecture that can enable or disable advanced capabilities. `litellm` availability is also checked.
*   **Warning Suppression:** Explicitly suppresses Pydantic serialization warnings from `litellm`.
*   **No significant functional changes were observed between these entries.** The content of the file remained identical across these three timestamps, suggesting either minor non-functional saves or re-indexing in the version control system.

### `d:\Projects\KolossusAI\orchestrator\guardrails.py` (5/18/2026, 6:36:52 PM)

This file defines various guardrail mechanisms to ensure AI responses are safe and adhere to policies.
*   **Key Functionality:**
    *   `guardrail_refusal`: Identifies and blocks responses that are primarily model refusals (e.g., "I cannot assist with"). It has a length heuristic to avoid blocking useful longer responses.
    *   `guardrail_llm`: Uses a separate LLM call to perform a safety check, being "VERY PERMISSIVE" and only refusing for "CLEARLY dangerous content." It records LLM usage.
    *   `guardrail_embedding`: Employs embedding similarity to detect dangerous topics (e.g., malware instructions, incitement to violence, self-harm). It utilizes either a self-hosted VLLM embedding service or falls back to `litellm`/OpenAI's `text-embedding-3-small`. It records LLM usage.
    *   `guardrail_identity_rewrite`: Checks if the response claims to be a specific AI (e.g., "I am Claude") and is designed to rewrite such statements using an LLM.
*   **Dependencies:** Relies on `litellm` for LLM and embedding calls, `django.conf.settings` for configuration (e.g., `ORCH_GUARDRAIL_MODEL`, `VLLM_ENABLED`, `KOLOSSUS_EMBEDDING_URL`), and `os` for environment variables.
*   **Logging:** Extensively uses `logger.warning` for triggered guardrails and `logger.error` for exceptions.
*   **Pattern:** Multiple guardrail functions follow a common pattern: check for `LITELLM_AVAILABLE`, fetch model/API configurations, perform a check (regex, LLM call, embedding similarity), log results, and return a boolean or modified text.

### `d:\Projects\KolossusAI\tools\schemas.py` (5/18/2026, 6:51:20 PM)

This file acts as a centralized registry for tool definitions within the KolossusAI system.
*   **Key Purpose:** Provides a "single source of truth" for tool schemas (OpenAI format), guides (Claude instructions), and metadata.
*   **`TOOLS` Dictionary:** Contains definitions for various tools, with `utility_tool` and `knowledge_base_search` being explicitly detailed in the provided log snippet.
*   **`utility_tool`:**
    *   **Description:** Mandated for file conversion and text extraction (e.g., `docx` to `pdf`, `pdf` to `txt`, `csv` to `xlsx`, archiving). Explicitly states "NEVER write custom Python or shell for conversion."
    *   **Actions:** `convert` (file to file) and `extract_text` (file to text).
    *   **Coverage:** Extensive list of supported formats (office docs, PDFs, data formats, images, archives, markdown).
    *   **Execution:** Marked as `sandbox_native`, meaning it runs inside the sandbox manager, not directly via Django, and provides `curl` examples for calling it.
*   **`knowledge_base_search`:**
    *   **Description:** Searches knowledge bases using semantic and keyword hybrid search. Emphasizes "ALWAYS search FIRST before saying you don't have information" and "always cite sources."
    *   **Parameters:** `query` (specific key terms) and `limit` (max results).
*   **System Constraints:** Highlights mandatory rules for all agents:
    *   Always use `utility_tool` for file conversion/extraction.
    *   Limit `shell_exec` to basic file operations; use `template_parser`/`document_generator` for content changes and `utility_tool` for format changes.
*   **Metadata:** Each tool includes `version`, `category`, `availability_rule`, `is_builtin`, `execution`, and `tags`.

### `d:\Projects\KolossusAI\workforce\tasks.py` (5/19/2026, 1:19:36 PM, 5/19/2026, 1:19:55 PM, and 5/19/2026, 1:33:59 PM)

This file defines Celery tasks for scheduled operations and message handling within the Workforce module.
*   **Key Functionality:**
    *   `on_celery_startup`: A Celery signal handler that runs on worker startup to check default tenant subscriptions, perform orchestrator session recovery, and clean up stale user-scoped pending approvals.
    *   `send_schedule_status`, `send_schedule_message`, `send_clear_chat`: Functions to send WebSocket updates (status, chat messages, clear chat signals) to specific workforce rooms.
    *   `_get_report_kb_context`: Retrieves knowledge base context for a report refresh, using the last user message as a query.
    *   `_parse_report_script_blocks`: Parses script content into structured blocks, handling various metadata lines (e.g., "# --- Script:", "# --- Sandbox:", "# --- Next Script Block ---").
    *   `_extract_verification_footer`: Extracts and removes a `__REPORT_OK__` verification marker from output text.
    *   `_extract_html_content`: Extracts HTML content from execution output, stripping markers and commentary.
    *   `_read_html_from_sandbox`: A fallback function to read `.html` files directly from the sandbox workspace.
*   **Dependencies:** Relies on `json`, `os`, `re`, `logging`, `requests`, `uuid`, `datetime.timedelta`, `asgiref.sync`, `channels.layers`, `decouple.config`, `celery` components (`shared_task`, `worker_ready`), Django models (`timezone`, `close_old_connections`, `Agent`, `AIModel`, `Tenant`, `User`, `Workforce`, `WorkforceMessage`, `ChannelMessageWorkforce`, `Group`, `WorkforceSchedule`, `ScheduleExecutionLog`, `ApprovalRequest`), and custom modules (`workforce.services`, `workforce.response_service`, `workforce.file_utils`, `workforce.tenant_config`, `workforce.message_formatting`, `orchestrator.utils`, `workforce.webhook_tasks`).
*   **Error Handling and Logging:** Includes `try-except` blocks for robustness and extensive logging for task status and errors, particularly during startup and WebSocket communication.
*   **Patterns:** Uses `async_to_sync` to bridge asynchronous Channels operations with synchronous Celery tasks. Regular expressions are used for parsing script blocks and HTML content.
*   **No significant functional changes were observed between these entries.** The content of the file remained identical across these three timestamps.

### `d:\Projects\KolossusAI\governance\enforcement.py` (5/19/2026, 1:37:03 PM)

This file implements the policy enforcement engine, featuring both LLM-based checks and system instruction injection.
*   **Core Concept:** Two layers of enforcement: LLM-based evaluation and system instruction injection.
*   **Helper Functions (`_get_default_ai_model`, `_get_default_llm_model`, `_get_workforce_owner_id`, `_get_workforce_tenant_id`, `_get_user_scope_rules`, `_get_tenant_scope_rules`, `_get_connector_rules`, `_get_all_rules`, `_get_workforce_connector_type_ids`, `_resolve_approval_target`):** A suite of functions to fetch AI model information, identify workforce ownership/tenancy, and retrieve various types of policy rules (user-scoped, tenant-scoped, connector-based) from the database, sorting them by priority. It also resolves who should review an approval request based on the rule's scope.
*   **`_check_remaining_uses`:** Checks if an `ApprovalRequest` has remaining uses, supporting permanent approvals (`-1`) and one-time approvals (`>0`). This function helps in determining if a policy violation has already been explicitly approved.
*   **`check_policies_with_llm`:**
    *   **Purpose:** The central function for LLM-based policy evaluation.
    *   **Process:** Gathers all active keyword match policy rules (tenant, user, connector scope), filters out those with permanent approvals, and then prepares to use an LLM to evaluate the user message and context against these rules.
    *   **Return:** Intends to return a list of violations, including the rule, action (`block` or `require_approval`), scope, and reason.
*   **Dependencies:** Relies on `django.utils.timezone`, `agent.models.AIModel`, `workforce.models.Workforce`, and its own models (`PolicyRule`, `ApprovalRequest`, `GovernanceRole`).
*   **Logging:** Uses `logger.info` for policy checks and `logger.error` for exceptions.

### Overall Patterns and Significant Changes:

1.  **Orchestrator Stability:** The `worker.py` and `workforce/tasks.py` files show identical content across multiple timestamps, suggesting that the primary functional logic within these files was stable during this period, or changes were internal and not reflected in these specific logged diffs (e.g., whitespace changes not captured, or external factors like re-commits without code changes).
2.  **Guardrail and Governance Focus:** The `guardrails.py` and `governance/enforcement.py` files indicate a strong emphasis on safety, policy enforcement, and intelligent moderation of AI responses. This includes:
    *   **Multi-modal Guardrails:** Using diverse techniques like regex matching (`guardrail_refusal`), a dedicated LLM for safety (`guardrail_llm`), and embedding similarity (`guardrail_embedding`) to cover different types of unsafe or undesirable content.
    *   **Identity Management:** `guardrail_identity_rewrite` specifically targets AI models claiming a particular identity.
    *   **Granular Policy Rules:** `governance/enforcement.py` implements a complex system for fetching and evaluating policy rules based on user, tenant, and connector scope, including priority levels and approval mechanisms (`ApprovalRequest`).
3.  **Tooling Formalization:** `tools/schemas.py` highlights a structured approach to defining AI tools. The `utility_tool` is a critical component for standardizing file operations, enforcing that agents use a unified tool instead of ad-hoc scripts for common tasks like conversion and extraction. This reduces complexity and ensures consistency.
4.  **Celery for Background/Scheduled Tasks:** `workforce/tasks.py` confirms the use of Celery for handling background operations like scheduled workforce execution, workspace cleanup, health checks, token flushing, and S3 backups, indicating a robust, scalable backend for managing AI workflows. The `on_celery_startup` signal demonstrates recovery and initialization logic at the worker level.
5.  **Modular Architecture:** The extensive imports across all files point to a highly modular and interconnected architecture, where different concerns (orchestration, tools, guardrails, governance, workforce management, logging, memory, etc.) are separated into distinct modules.
6.  **Future-proofing/Conditional Features:** The conditional imports for PII protection, multi-agent routing, and graph memory in `worker.py` suggest an architecture designed to easily incorporate or activate advanced features as needed.
7.  **Database and Redis Integration:** The presence of Django ORM calls (`select_for_update`, `aggregate`, `filter`, `create`, `update`, `save`) and Redis utilities (`redis_utils` module in `worker.py`) indicates reliance on both a relational database and Redis for persistence and real-time/caching needs.