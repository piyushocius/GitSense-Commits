# Activity Summary for 5/20/2026

## 3:54:24 AM
The provided log details significant code additions and updates across several core components of the KolossusAI project between May 18th and May 19th, 2026.

**File-Specific Updates:**

*   **`d:\Projects\KolossusAI\orchestrator\worker.py`** (First seen: May 18, 2026, 6:29:37 PM)
    This file, a central orchestrator component, was introduced or heavily updated. It includes an extensive set of imports covering logging, Pydantic warning suppression, Django database utilities, context management, entity and fact handling, goal management, memory systems, core models (`OrchSession`, `OrchTurn`, `OrchPlan`), planning, prompt building, retrieval, Redis utilities for task management, AI response generation, summarization, tool execution, usage tracking, logging, workforce interactions, vector storage, fact/policy checking, and various guardrails.
    Key functions defined are:
    *   `create_tool_turn_safe`: Ensures atomic creation of `OrchTurn` records with row locking to prevent race conditions.
    *   `_sanitize_text`: Trims and limits text to a specified character count.
    *   `_get_model_pricing`: Retrieves AI model pricing and display names from the database, with fallback mechanisms and static pricing.
    *   `_clean_file_links_in_response`: Modifies markdown file links in AI responses, removing duplicates for attached files or replacing relative links with download URLs.
    *   `_normalize_chart_output`: Standardizes various chart JSON output formats into a consistent ````chart { ... }``` block.
    *   The file also sets up availability checks for PII protection, multi-agent routing, graph memory, and LiteLLM.

*   **`d:\Projects\KolossusAI\orchestrator\guardrails.py`** (First seen: May 18, 2026, 6:36:52 PM)
    This new file introduces robust guardrail mechanisms for AI responses:
    *   `guardrail_refusal`: Identifies and blocks responses that are primarily self-refusals by the model (e.g., "I cannot assist with").
    *   `guardrail_llm`: Utilizes an external LLM (e.g., `gpt-4o-mini`) to perform a permissive safety check for clearly dangerous content (weapons, incitement to violence, stolen data), recording LLM usage.
    *   `guardrail_embedding`: Employs embedding-based cosine similarity to detect responses highly similar to dangerous topics (malware, self-harm), supporting both self-hosted VLLM and LiteLLM/OpenAI embeddings.
    *   `guardrail_identity_rewrite`: Rewrites AI responses that claim a specific identity (e.g., "I am Claude") using an LLM.

*   **`d:\Projects\KolossusAI\tools\schemas.py`** (First seen: May 18, 2026, 6:51:20 PM)
    This file establishes a unified registry for tool definitions, providing OpenAI-compatible schemas, Claude-specific guides, and metadata for each tool.
    *   It defines a critical `utility_tool` for universal file conversion and text extraction, emphasizing its mandatory use for such tasks and explicitly forbidding custom Python or shell scripts for these operations. The guide details extensive coverage for various document, data, image, and archive formats.
    *   It also includes the `knowledge_base_search` tool for querying workforce-attached documents.
    *   System constraints are outlined, mandating `utility_tool` for file operations and restricting `shell_exec` to basic file system commands.

*   **`d:\Projects\KolossusAI\workforce\tasks.py`** (First seen: May 19, 2026, 1:19:36 PM)
    This file contains Celery shared tasks for managing workforce scheduling and operations.
    *   It includes `on_celery_startup` which connects to the `worker_ready` signal to ensure default tenant subscriptions and execute session recovery at startup. It also cleans up stale user-scoped approval requests that may have timed out due to server restarts.
    *   Functions `send_schedule_status`, `send_schedule_message`, and `send_clear_chat` are defined to facilitate real-time communication (status updates, chat messages, and chat clearing) to WebSocket rooms for workforces.
    *   Several helper functions for report generation are present: `_get_report_kb_context` (retrieving knowledge base context), `_parse_report_script_blocks` (parsing code from reports), `_extract_verification_footer` (processing specific output markers), `_extract_html_content` (extracting HTML from output), and `_read_html_from_sandbox` (a fallback for reading HTML files from the sandbox).

*   **`d:\Projects\KolossusAI\governance\enforcement.py`** (First seen: May 19, 2026, 1:37:03 PM)
    This new file introduces a comprehensive policy enforcement engine with two main layers: LLM-based checks and system instruction injection.
    *   Helper functions retrieve default AI models, workforce owner and tenant IDs, and various active policy rules (user-scoped, tenant-scoped, and connector-based).
    *   `_get_all_rules`: Aggregates and sorts all active policy rules by priority.
    *   `_resolve_approval_target`: Determines the appropriate user or role for reviewing policy approval requests.
    *   `_check_remaining_uses`: Manages one-time or permanent policy approvals by tracking and decrementing remaining uses for tenant-scoped policies.
    *   `check_policies_with_llm`: The core LLM-based policy evaluation function, which assesses user messages against active keyword policies, considering any pre-approved exceptions, and identifies potential violations.

**Timestamps of Significant Changes:**

*   **May 18, 2026, 6:29:37 PM**: Initial or major commit for the core `orchestrator\worker.py` functionality.
*   **May 18, 2026, 6:36:52 PM**: Introduction of `orchestrator\guardrails.py`, marking a clear focus on AI safety and content moderation.
*   **May 18, 2026, 6:51:20 PM**: Introduction of `tools\schemas.py`, establishing a standardized tool registry and critical `utility_tool`.
*   **May 19, 2026, 1:19:36 PM**: Introduction of `workforce\tasks.py`, detailing Celery tasks for workforce management and real-time communication.
*   **May 19, 2026, 1:37:03 PM**: Introduction of `governance\enforcement.py`, outlining a sophisticated policy enforcement system.

**Patterns and Recurring Elements:**

*   **Modularity and Clear Responsibilities:** The changes reflect a highly modular architecture, with new files like `guardrails.py`, `schemas.py`, `tasks.py`, and `enforcement.py` each addressing specific functionalities (safety, tool definition, background tasks, governance).
*   **AI Orchestration Focus:** All changes contribute to a robust AI orchestration system, integrating LLM functionalities, tool use, memory, planning, and safety checks.
*   **Real-time Interaction:** The `workforce\tasks.py` additions highlight an emphasis on real-time user feedback and status updates via WebSockets.
*   **Policy and Governance:** The introduction of `governance\enforcement.py` alongside `orchestrator\guardrails.py` indicates a strong commitment to enforcing policies, safety, and responsible AI usage, utilizing both keyword matching and LLM-based evaluation.
*   **Tool Standardization:** The `tools\schemas.py` file centralizes tool definitions and imposes strict guidelines on tool usage (e.g., always using `utility_tool` for file conversions).
*   **Duplicate File Entries:** A notable pattern is the presence of multiple log entries for `orchestrator\worker.py`, `tools\schemas.py`, and `workforce\tasks.py` at different timestamps, where the code content remains identical. This suggests frequent save operations or automated processes updating file timestamps without corresponding functional code changes in those specific instances.
*   **Exclusion of Sensitive Information:** The log correctly omitted details from `.env` files, which aligns with best practices for handling sensitive data.

## 12:54:29 PM
**File: d:\Projects\KolossusAI\orchestrator\worker.py**
*   **Timestamp:** 5/18/2026, 6:29:37 PM, 5/18/2026, 6:30:43 PM, 5/18/2026, 7:10:06 PM, 5/19/2026, 12:55:14 PM
*   **Updates:** This file serves as a core orchestrator component, importing a wide range of utilities for AI agent functionality. Key functions include:
    *   `create_tool_turn_safe`: Ensures atomic creation of `OrchTurn` records for tool outputs, using database row locking to prevent race conditions during concurrent operations.
    *   `_sanitize_text`: A utility to clean and truncate text, enforcing a maximum character limit.
    *   `_get_model_pricing`: A helper to dynamically retrieve AI model pricing and display names from the database or static configurations, handling various provider prefixes (e.g., "openai/", "google/").
    *   `_clean_file_links_in_response`: Modifies markdown file links in LLM responses. It removes links if the file is already attached or replaces them with a download URL if not, and handles ambiguous relative links.
    *   `_normalize_chart_output`: Standardizes different chart data formats found in LLM outputs into a consistent ````chart { ... } ```` block.
*   **Patterns:** The code demonstrates extensive use of Django models and utilities, Redis for lease management, and conditional imports for features like PII protection, multi-agent routing, and graph memory, indicating a modular and configurable architecture. Multiple consecutive commits show identical code, suggesting frequent saves without functional alterations.

**File: d:\Projects\KolossusAI\orchestrator\guardrails.py**
*   **Timestamp:** 5/18/2026, 6:36:52 PM
*   **Updates:** This file implements various guardrail mechanisms for LLM responses:
    *   `guardrail_refusal`: Identifies and flags responses that are solely a model's refusal message (e.g., "I cannot assist with"), primarily for shorter responses.
    *   `guardrail_llm`: Utilizes a separate LLM (defaulting to `gpt-4o-mini`) to evaluate `reply_text` for clearly dangerous content (e.g., instructions for weapons, stolen financial data). It's designed to be highly permissive, only blocking obvious safety violations. LLM usage is recorded.
    *   `guardrail_embedding`: Employs embedding similarity to detect dangerous content. It embeds the response text and a set of predefined dangerous topics, refusing if cosine similarity exceeds a high threshold (0.85). It supports self-hosted VLLM for embeddings as a primary option, with LiteLLM as a fallback. LLM usage for embedding generation is tracked.
    *   `guardrail_identity_rewrite`: Checks for specific first-person identity claims (e.g., "I am Claude") in responses and uses an LLM to rewrite the text to remove such claims, if LiteLLM is available.
*   **Patterns:** The guardrail system uses a multi-faceted approach, combining rule-based checks, LLM-based content evaluation, and embedding similarity for robust content moderation and identity management. There's a clear emphasis on recording LLM usage for these checks.

**File: d:\Projects\KolossusAI\tools\schemas.py**
*   **Timestamp:** 5/18/2026, 6:51:20 PM, 5/19/2026, 1:59:48 PM, 5/19/2026, 2:05:44 PM
*   **Updates:** This file defines a unified registry (`TOOLS`) for all available AI tools, including their OpenAI-style schemas, detailed guides (instructions for Claude-like agents), and metadata.
    *   **System Constraints:** Explicitly defines two mandatory rules for agents:
        1.  **File Conversion & Extraction:** Agents *must* use `utility_tool` for any file conversion or text extraction, prohibiting custom Python or shell scripts for these tasks. A comprehensive list of supported formats is provided.
        2.  **Shell Limits:** `shell_exec` is restricted to basic file operations (e.g., `ls`, `cp`, `mv`), with `template_parser` or `document_generator` for content changes and `utility_tool` for format changes.
    *   **`utility_tool` Definition:** Provides a detailed schema and guide for a "sandbox-native" tool that unifies file conversion and text extraction. The guide specifies its two actions (`convert`, `extract_text`), extensive coverage, examples, response contract, and emphasizes direct `curl` calls to a `SANDBOX_MANAGER_URL` rather than the Django API.
    *   **`knowledge_base_search` Definition (partial):** Includes schema for querying knowledge bases, emphasizing searching first and citing sources.
*   **Patterns:** The file establishes a "single source of truth" for tool definitions, promoting consistent and controlled agent behavior. The detailed `guide` sections are tailored for instructing large language models (like Claude), and the strict system constraints aim to enforce safe and efficient tool usage, particularly for file operations within a sandbox environment. Identical code submissions indicate frequent saving.

**File: d:\Projects\KolossusAI\workforce\tasks.py**
*   **Timestamp:** 5/19/2026, 1:19:36 PM, 5/19/2026, 1:19:55 PM, 5/19/2026, 1:33:59 PM
*   **Updates:** This file contains Celery tasks for scheduling and managing various workforce operations:
    *   **`on_celery_startup`:** A signal handler that runs when a Celery worker starts. It ensures default tenant subscriptions are active, initiates orchestrator session recovery, and cleans up stale user-scoped pending approval requests (those older than 2 minutes are timed out).
    *   **WebSocket Communication Functions:** `send_schedule_status`, `send_schedule_message`, and `send_clear_chat` provide utilities to send real-time updates and messages to specific workforce WebSocket chat rooms via `channels.layers`.
    *   **Report Generation Utilities:**
        *   `_get_report_kb_context`: Retrieves knowledge base context relevant to a report refresh, using the last user message as a query.
        *   `_parse_report_script_blocks`: Parses report script content, identifying and separating code blocks based on custom markers (`# --- Script:`, `# --- Sandbox:`, `# --- Next Script Block ---`).
        *   `_extract_verification_footer`: Removes a `__REPORT_OK__` marker, used for enhanced verification in "claude_code" mode.
        *   `_extract_html_content`: Extracts clean HTML content from raw execution output, stripping internal markers and commentary.
        *   `_read_html_from_sandbox`: A fallback function to directly read HTML files from a sandbox workspace.
*   **Patterns:** The file highlights asynchronous processing using Celery, real-time communication via WebSockets, and structured handling of report generation, including code parsing and content extraction. Multiple identical code submissions again suggest frequent saves.

**File: d:\Projects\KolossusAI\governance\enforcement.py**
*   **Timestamp:** 5/19/2026, 1:37:03 PM
*   **Updates:** This file defines the policy enforcement engine, employing two main layers: LLM-based checks and system instruction injection.
    *   **Helper Functions for Rules & Approvals:** A suite of private functions (`_get_default_ai_model`, `_get_default_llm_model`, `_get_workforce_owner_id`, `_get_workforce_tenant_id`, `_get_user_scope_rules`, `_get_tenant_scope_rules`, `_get_connector_rules`, `_get_all_rules`, `_get_workforce_connector_type_ids`, `_resolve_approval_target`) handle fetching active policy rules, identifying workforce context, and determining the appropriate target for approval requests based on rule scope (user, tenant, connector).
    *   **`_check_remaining_uses`:** Checks for active `ApprovalRequest` records. If an approval has `remaining_uses` > 0, it is decremented and the action is allowed. If `remaining_uses` is -1, the action is always allowed. This is primarily for tenant-scoped one-time approvals.
    *   **`check_policies_with_llm`:** The primary function for LLM-based policy evaluation. It takes a user message, system instruction, recent messages, and workforce/tenant IDs. It fetches all active keyword policies, filters out those with permanent approvals, and then uses an LLM to determine if the input violates any remaining policies. It returns a list of detected violations.
*   **Patterns:** The governance system is designed with multiple scopes (user, tenant, connector) and a clear priority order for policy rules. It integrates LLMs for flexible policy checking and includes mechanisms for time-limited or permanent approvals, indicating a robust and adaptable governance framework.

**File: d:\Projects\KolossusAI\scripts\create_section_writer_agents.py**
*   **Timestamp:** 5/20/2026, 11:35:36 AM
*   **Updates:** This is a one-off script for creating a specific AI agent, "EIA Section Writer - Agent A," for a hardcoded user (`piyush.ranjan@ociustechnologies.com`).
    *   **`AGENT_A_INSTRUCTION`:** Contains an extremely detailed set of "GROUND RULES" for Agent A. These rules dictate:
        *   The agent's role (filling Introduction, Project Description, Summary & Conclusions sections of an EIA Report from a Project Feasibility Report).
        *   Strict formatting: no modifying headings, no section numbers in text, specific handling of empty sections, use of real Word tables, and clean black text output.
        *   Tooling and content source restrictions: explicitly forbids `template_parser`, requires custom Python for file operations, prohibits web searches, disallows sub-agents, and limits content sourcing to specific sections of the PFR.
        *   Detailed instructions for handling existing content (static text, tables, red instructions), avoiding repetition, and section-specific content sourcing logic (e.g., Introduction only from PFR Introduction).
        *   Precise rules for inserting and numbering tables, figures, and photographs, including dynamic counter initialization based on existing document content to ensure sequential numbering.
        *   Permission to add mini subsections.
*   **Patterns:** This script demonstrates the capability to define highly specialized AI agents with extremely granular and prescriptive instructions. The extensive `AGENT_A_INSTRUCTION` highlights the complexity of guiding AI agents in structured document generation tasks, emphasizing strict adherence to formatting, content sourcing, and numbering conventions.